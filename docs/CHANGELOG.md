# Changelog

Reverse chronological. One line per commit. Per the Documentation
Update Rule in `CLAUDE.md`, every commit should append an entry
here. Group multi-commit days under a single date heading. Use
`git show <hash>` for the full diff of any entry.

Format: `commit-hash | brief description`

---

## 2026-07-13

- `(pending)` | **PALS Checkpoint B, code half — "configure existing day" generator mode (Task Handoff Queue).** `generateAhaCourseForCohort` (Checkpoint A) is deliberately INSERT-only — no path to attach a template's structure onto a `lab_days` row that already exists. Adds `configureExistingCohortDay` (`lib/aha-course-generator.ts`) + `POST/GET /api/admin/aha-courses/configure-day` + a "Configure an existing day" section on `/admin/aha-courses` (same page as Checkpoint A's generator — no new nav entry needed) that fills that gap **without relaxing the guardrail**: every write is a genuine gap-fill (field/station was null, now set) or genuine addition (template station with no counterpart on the day); anything that would overwrite an existing value is reported as a `conflict_skipped` and left alone. Repeatable by construction — works against any existing `lab_days` row + any template, not a G14 special case. Dry-run by default (explicit `dry_run:false` required to write), matching Checkpoint A's convention. **Not yet applied to G14's real PALS days** — see the same-day Session Log entry flagging Ben for go/no-go on the actual write (production content ~4 days out; also surfaces that the 4 existing `lab_day_templates` "certification" rows still carry `cert_course=NULL` from Checkpoint A — a config gap this mode alone can't fix since it only fills day-level gaps *from* the template's own values). `npm run type-check` and `npm run build` clean; `npx eslint` clean on all new/touched code (the one pre-existing hoisting error + hook-dep warning on `app/admin/aha-courses/page.tsx` predate this change — confirmed via `git stash`).
- `ba4224a5` | **PALS Checkpoint C — live testing grading UI (Task Handoff Queue).** Builds the grading UI per `docs/pals/PALS_Grading_Model_Spec.md` on top of the `pals_*` schema shipped 2026-07-12 (no migration — schema unchanged). New: `app/labs/pals/grade/page.tsx` (testing day → group → Team Leader + members → PALS scenario, gated on `narrative_status='complete'`), `lib/pals.ts` + `types/pals.ts` (mirrors the `lib/adv-cert.ts` split), `app/api/pals/{scenarios,scenarios/[id],grading-context,attempts}/route.ts`, `components/pals/PalsCaseCard.tsx` (renders the OCR'd AHA narrative card). Grading model matches the spec exactly: flat Critical Performance Steps (not the ACLS segment chain), three-state checked/not_checked/na per criterion (na = out-of-scope, not a blank), verbatim `instructor_prompt`, `is_critical` flagging, PASS/NR **computed** client-side from the AHA rule (all in-scope steps checked) rather than instructor-picked, `retest_of` linkage against open NR attempts, server-computed 30-day `remediation_due_at` (never trusts a client clock). **Offline** (`lib/pals-offline.ts`): the ACLS megacode station never built real local persistence beyond the `client_uuid` dedup key (see 2026-06-xx entry, "Pending: ...then offline phase") — this ships it for real: in-progress form state auto-persists to `localStorage` keyed by a client-minted `client_uuid` and restores on reload, a failed save queues locally and auto-flushes on reconnect via the existing `useOffline()` provider. Discipline-based N/A: `students.discipline` is null for all 173 current students (verified live, not assumed), so no auto-guessed per-criterion N/A default was built — fabricating a discipline→criterion mapping with no real data would violate the Data Integrity rules; instead the UI shows the team lead's discipline plus the case's own `paramedic_scope_na_candidates` narrative hint (real seeded data) as a cross-reference, and N/A stays an explicit one-tap instructor action. Nav: Labs hub tile ("PALS Testing Grading") + a PALS-aware branch on the per-station Grade button (`components/lab-day/StationCards.tsx`, `certCourse` prop) alongside the existing ACLS branch. Verified against live production data before building (not assumed): all 12 checklists/150 criteria present, 16/32 PALS scenarios `narrative_status='complete'` (10 practice + 6 testing, matching the task text), 0 `lab_days` currently tagged `cert_course='pals'` (Checkpoint B is the follow-up that creates the first ones). `npm run type-check` and `npm run build` clean; `npx eslint` on all new/touched files clean (pre-existing `no-explicit-any`/hook-dep warnings in files this touched incidentally, e.g. `app/labs/schedule/[id]/page.tsx`, are unrelated pre-existing debt, confirmed identical on the equivalent pre-existing `lib/adv-cert.ts`/`app/api/adv-cert/grading-context/route.ts` pair). Docs updated same commit: `SITEMAP.md` (new route), `DATABASE_SCHEMA.md` (consumer note + live narrative_status recount).
- `50a9c51`, `13a23bc` | **Merged 2 stranded CI-green draft PRs (Task Handoff Queue).** Both were open drafts with `mergeable_state: clean` and green Vercel builds, sitting unmerged the way PR #23/#30 previously did. Merged PR #35 (AHA-generic course template + generator, Checkpoint A — see entry below) and PR #29 (LVFR calendar-sync fallback time + seed clobbering `time_blocks` fix — commits `4b7a0ff`/`3c6bd64`, entered in the 2026-07-11 section). No other open PRs remained afterward.
- `2aa5146` | **AHA-generic course template + generator, Checkpoint A (Task Handoff Queue, Ben approved "PALS BUILD" design).** First checkpoint of the approved AHA-generic template+generator build: adds `lab_day_templates.{cert_course,section_number,section_label,is_adv_cert_testing,lab_mode}` (migration `20260713_aha_generic_course_templates.sql`, additive/nullable, reuses the existing `lab_day_templates`/`lab_template_stations` tables rather than a new one — the 4 existing "certification"-category stub rows and their 1:1 links to real G14 lab_days, including 2 upcoming PALS placeholders dated 2026-07-16/17, are untouched) and a new generator (`lib/aha-course-generator.ts`, `POST /api/admin/aha-courses/generate`, UI at `/admin/aha-courses`) that expands a tagged template into a target cohort's own `lab_days`/`lab_stations`, setting `source_template_id`. Hard guardrail: if a `lab_days` row already exists at the same `(cohort_id, date, section_number)`, it is reported as skipped and never touched — verified directly against production by simulating the exact query against G14's real `2026-06-18` section-1 row (`9df60c8b`) and confirming it resolves to `skip_existing`, plus independently verified the task's 2 protected row IDs (`aebf842d` = G14 Day-2 Megacode Practice Set 1, `26e3efe1` = Set 2 — not documented anywhere in the repo before this session) each carry 16 real `adv_cert_test_attempts` (32 total, matching the task text) before writing any code. Caught and fixed a real bug pre-commit: `lab_days.section_number` is `NOT NULL DEFAULT 1` (never actually null) while the new template column is nullable — an early version would have tried to insert a literal `NULL` and violate the constraint; normalized to default 1 in both the existing-row check and the insert. Full wizard (date-pick, auto-swap cohort didactic/lab days, instructor holds, per-instructor assignment, part-timer self-request) explicitly deferred per the approved design. Checkpoints B (PALS minimal setup) and C (PALS grading UI) are separate follow-up work — PALS Phase 1's data layer (`pals_checklists`, `pals_test_attempts`, etc.) already shipped 2026-07-12 in commit `dc1e0234`. `npm run type-check` and `npm run build` clean. Flagged to Ben separately: G14 already has placeholder PALS Day 1/Day 2 `lab_days` scheduled 2026-07-16/17 (4 days out) with no real content, and the narrative-extraction content gate (29/32) is still unowned.

## 2026-07-12

- `(pending)` | **Archive 2 leftover ACLS lab_days rows via new `lab_days.is_archived` flag (Task Handoff Queue, Ben approved).** Ben spotted a "free-floating megacode" in the general lab schedule — a stray G14 (cohort `8577fdc3`) ACLS Day-2 lab_days row surfacing with no real use. Added `lab_days.is_archived` (additive, migration `20260712_lab_days_is_archived.sql`, same archive-not-delete convention as `cohorts.is_archived`) and set it `true` on 2 rows, both independently verified (not just taken from the queue item text) to have 0 `adv_cert_test_attempts`: `5c7f74f6` (section 1, already relabeled `[ARCHIVED]` in the 2026-06-19 Day-2 restructure — the 16-station monolith superseded by the current Practice Set 1/Set 2 + paired Testing sections) and `85808c0a` (section 3, "Megacode Testing (Scored)" — carries 4 real station/instructor assignments but never actually recorded an attempt, since G14's 32 real attempts all came from the two Megacode Practice sections instead, which AHA permits as practice-can-serve-as-testing). Archiving only hides them from list views — no data deleted, either row can be unarchived instantly. Updated both list queries that were missing an archived filter: `/api/lab-management/lab-days` (general lab schedule) and `/api/adv-cert/acls-hub` (including its 2 cohort-auto-detect sub-queries). `npm run type-check` and `npm run build` clean; `npx eslint` on both touched files shows only pre-existing unrelated errors (confirmed identical error count on the pre-change file via `git stash`).

- `02abe34d` | **PALS narratives loaded (Cowork delivery, Task Handoff Queue).** Copied the updated `pals_scenario_seed.json` + the 2 new narrative files (`pals_card_narratives_PRACTICE.json`, `pals_card_narratives_TESTING.json`) + 2 content-guard specs into `docs/pals/` (exemplar left as-is to preserve its `scenario_practice` alignment). Added `scenarios.pals_narrative` jsonb (migration `20260712_pals_narrative.sql`) and widened the `narrative_status` CHECK for the seed's new `INCOMPLETE_SCAN_GAP` value (`20260712_pals_narrative_status_scan_gap.sql`). Enhanced `scripts/import-pals-seed.js` to load the narrative cards verbatim keyed by `case_code`. Result: **16 scenarios now `narrative_status='complete'`** (was 1), all 16 carry loaded narrative content (18 narratives loaded total); the 3 program testing cards (T7/T9/T13) + the 10-scenario practice set now pass the `narrative_status=='complete'` display gate. No fabrication — completeness follows the seed (the 2 gap-flagged cards stay `INCOMPLETE_SCAN_GAP`, gated out).
- `(pending)` | **Re-apply stranded draft-PR #30 content directly to main (Task Handoff Queue, Ben authorized).** PR #30 bundled a data-safety guard on the LVFR AEMT planner reseed wipe hazard and a sync of data-integrity rules into `CLAUDE.md`, both already marked Done on the queue, but the PR itself was never merged — left stranded as an unmerged draft, so clones reading only `main` never got either change. Re-applied both directly as clean commits: (1) **Data-safety guard on `POST /api/lvfr-aemt/planner/reseed`** (and its `scripts/reseed-lvfr-schedule.js --direct` twin) — the route unconditionally DELETEd and reinserted all `lvfr_aemt_plan_placements` rows for the plan instance from a hardcoded 30-day `SCHEDULE`; it now refuses to run without `{"confirm": true}` (dry-run preview otherwise) and snapshots every existing row into a new `lvfr_aemt_plan_placements_backups` table before the delete, hard-aborting if the snapshot insert fails. Migration `20260712_lvfr_plan_placements_backups.sql` (additive/idempotent) re-applied to the repo — the table itself was already live in production (applied via Supabase MCP during the original PR #30 session), confirmed present before re-adding the file. (2) **`CLAUDE.md` sync**: added the Data Integrity Operating Rules section (no fabricated/inferred/pattern-guessed data, investigate existing DB structure first, authoritative-source-only direct writes with a backup, diagnose from real data not guesses, reversibility is not a license to guess), strengthened Claim-first step 1 to also check for an existing open PR on the same area before claiming, and added the Crunch/Overnight Autonomy section (don't idle on a blocked item, don't lower the safety bar, merge only bounded/verified fixes solo, flag blocked items with what's needed from Ben). Draft PR #30 closed as superseded (its content is now on `main` via this commit). `npm run type-check` and `npm run build` verified clean before commit.

- `bcaecd10` | **docs(CLAUDE.md): add Site Scope + Scope-Check (HARD REQUIREMENT) (Task Handoff Queue).** Documents Ben's definition of what the site IS (a cohort-EMS-education tracker/scheduler; primary job = aggregate many areas into one view), what it DOES vs DOESN'T (Platinum Planner owns individual skill-proficiency + clinical/internship scheduling), the AHA scenario/day-tracking exception, and a scope-CHECK behavior: if a build seems out-of-scope, ASK Ben about expanding/exception rather than silently building or refusing. Scope is updatable. Bounded additive doc change.
- `a29174c` | **docs(CLAUDE.md): add Repeatability / Build-Modular Rule (HARD REQUIREMENT).** Codifies Ben's standing directive into the shared rulebook (previously only in Code's private memory, invisible to the other agents): before building, run a repeatability check — if a thing recurs across cohorts/courses/sessions build it cohort-scoped/repeatable/UI-editable, not a hardcoded one-off; applies especially to schedule features; flag when unsure rather than defaulting to a one-off. Companion to Archive-don't-delete + Data Safety.
- `dc1e023` | **PALS Phase 1 — schema + grading engine + importer + cert_tier rename (Task Handoff Queue, local Code session).** Added the PALS grading module (its OWN model, NOT an `adv_cert_*` content load): migration `20260712_pals_module.sql` creates `pals_checklists` / `pals_checklist_criteria` / `pals_test_attempts` / `pals_attempt_students` / `pals_criterion_results` / `pals_skill_completions` (uuid PKs, RLS, `-- ROLLBACK:` block) and adds `scenarios.narrative_status` (display gate), `scenarios.pals_checklist_id` (FK), `scenarios.pals_case_meta` (jsonb, verbatim seed), `students.discipline`. Importer `scripts/import-pals-seed.js` loaded 12 checklists + 150 criteria (verbatim AHA PALS 2025) + 32 scenarios (16 `scenario_practice` + 16 `scenario_testing`; 28 linked, 4 TBD unlinked; narrative_status 1 complete / 29 pending / 2 not_indexed). **Coordinated cert_tier rename** (`20260712_cert_tier_scenario_rename.sql`, `--backup=scenarios`): `megacode_practice|megacode_testing` → `scenario_practice|scenario_testing` shared across ACLS+PALS — 14 ACLS `scenarios` rows migrated + 3 CHECK constraints swapped, with code refs updated in lockstep (`types/adv-cert.ts`, `lib/adv-cert.ts`, `app/api/adv-cert/scenarios/route.ts`, `app/labs/adv-cert/grade/page.tsx`, 2 diag scripts). Verified: ACLS grading pool (14) + `adv_cert_test_attempts` (32) intact; no `megacode_*` left. Snapshot `_backup_scenarios_20260712195515` retained. Design deviation (documented): checklists keyed by pathophysiology, not 1:1 per scenario. Phase 2 (grading UIs) gated on `narrative_status='complete'`.
- `5aacb4ed` | **docs(pals): staged the 6 authoritative PALS handoff files into `docs/pals/`** (scenario seed, grading model spec, practice exemplar, 2025 testing/setup changes, Module 6 schedule breaks, manikin-constrained case matrix) so cloud clones can continue Phase 2. Skipped the stale DO-NOT-SEND files.
- `72da370` | **LVFR planner: editable, self-service instructor assignment (any `lab_users` instructor) (Task Handoff Queue, PR #31).** The planner's instructor assign/unassign UI (`BlockBar`'s dropdown in `app/lvfr-aemt/planner/page.tsx`) was already built and wired to `plan_placements.instructor_id -> lab_users`, which already supports any instructor — the actual gap was that the dropdown's candidate list was derived from the `/api/lvfr-aemt/planner/availability` feed, which `lib/lvfr-availability.ts` only computes for a hardcoded name match (Ben/Jimi/Trevor), so anyone else (e.g. Ryan) never appeared as assignable. Added `GET /api/lvfr-aemt/planner/instructors` (lists all active instructor-eligible `lab_users` directly, independent of availability) and pointed the dropdown at it instead. Also fixed `PUT /api/lvfr-aemt/planner/placements` to resolve+persist `instructor_name` server-side from `instructor_id` so the cached name can't go stale. Purely additive — no schema change, `lvfr_aemt_instructor_assignments` (the separate table backing live calendar sync) and its readers untouched. tsc clean, build clean, eslint diff-clean. The hardcoded 3-name `InstructorKey` union in the reseed route and the availability name-matcher itself remain out of scope (separate, later, Ben-gated consolidation) per the task.

## 2026-07-11

- `4b7a0ff` | **Fix LVFR calendar-sync fallback time + seed clobbering `time_blocks` (Task Handoff Queue).** Two compounding bugs behind "LVFR events synced at wrong evening time": (1) `lib/google-calendar.ts`'s `PROGRAM_TIME_DEFAULTS['AEMT']` fell back to `18:00-21:30` for any day with no explicit start/end time, but the LVFR AEMT course runs `07:30-15:30` — changed the default so a null-`time_blocks` day lands on the real class time. (2) `app/api/lvfr-aemt/admin/seed/route.ts` upserted `time_blocks: d.time_blocks || null` unconditionally; `data/lvfr-aemt/course_calendar.json` never carries that field (confirmed 0/30 days), so every re-run of the seed endpoint silently nulled out whatever `time_blocks` had been set directly in the DB — including the fix Claude AI had just applied and backed up. Now omits the key entirely when the JSON doesn't supply it, so `ON CONFLICT DO UPDATE` leaves the existing DB value alone. Display-time only, no schema change. tsc clean, build green (fresh `npm install` in this session).
- `7a67624` | **Merge backlog: land the 2 still-pending launch-critical fixes from draft PR #23 (general-lab scope correction + LVFR additional-instructor wiring).** PR #23 bundled three items; its RT/ACLS-instructor-exclusion commit (`4b7fd18`) was already deployed separately on 2026-07-09 (`bc600429`, PR #24), so PR #23 had gone stale/conflicting (`lib/rt-only-instructors.ts` collided with the already-merged copy) while its other two fixes — genuinely still absent from `main` — sat blocked behind that collision. Cherry-picked only commit `7a67624` (untouched by any later merge: `lib/general-lab-sync.ts` scope fix — station/role assignment is now the only thing that suppresses the generic lab event, no longer a didactic class-date "teach-link"; broadened eligibility from full-time to active — and `app/api/admin/calendar-sync/route.ts` — Sync All now also iterates `lvfr_aemt_instructor_assignments.additional_instructors`, previously silently skipped) onto current `main` from a fresh branch; applied without conflict. PR #23 itself closed without merging (superseded — its mergeable content is now on `main` via this commit + `bc600429`). Per the source PR's own instruction, **not yet flipped to Done on the Task Handoff Queue** — "Sync All" still needs to be run with production Google OAuth credentials (unavailable in this sandbox) and Ben's calendar verified against the corrected logic before the underlying queue items are closed out. tsc/build not re-run in this session (no `node_modules`); the cherry-picked commit's own tsc-clean/build-clean verification from its original session stands, and the diff introduced zero new code beyond what that verification covered.

## 2026-07-10

- `103b101` | **Fix LVFR `course_calendar.json` drift for Days 15-17 (Task Handoff Queue, verify DB vs static JSON).** Confirmed the live LVFR calendar display (`GET /api/lvfr-aemt/calendar`) reads `lvfr_aemt_course_days` directly from Supabase — no static JSON in that read path, so the display was never wrong. However `data/lvfr-aemt/course_calendar.json` is also a live seed source (`POST /api/lvfr-aemt/admin/seed`, admin+ role, upserts `lvfr_aemt_course_days` `ON CONFLICT DO UPDATE`) and still held the pre-restructure values for these 3 days, so re-running that endpoint would have silently reverted the correct DB state back to stale data. Updated the JSON to match the DB exactly: Day 15 (8/13) is lecture-only (no lab/exam), Day 16 (8/18) carries the Trauma Module exam, Day 17 (8/19) carries both trauma labs (C-Spine + Splinting). Verified all other 27 days already matched the DB — no further drift in this file. Separately flagged (not fixed, high-stakes/Ben): `app/api/lvfr-aemt/planner/reseed/route.ts` and `scripts/reseed-lvfr-schedule.js` contain a completely different, stale hardcoded 30-day schedule (dates ~1 week off, missing the added review days) that would badly corrupt the whole calendar if that admin-role reseed endpoint were ever called — see Session Log / Task Handoff Queue.
- `a8fe74e` | **Fix: full-time instructors wrongly labeled "Volunteering" on the lab-day available-instructors list (Task Handoff Queue: Rae).** Root cause: `app/api/lab-management/instructor-availability/route.ts`'s volunteer-signal logic treated ANY `station_instructors` row on the target lab_day as a volunteer sign-up, but `station_instructors` is the generic "assigned to teach this station" table (written by the normal `EditStationModal` flow via `PATCH /api/lab-management/stations/[id]` / `POST /api/lab-management/station-instructors`) with no column distinguishing a volunteer sign-up from an ordinary teaching assignment. Verified live: Rae is a recurring, normally-assigned station instructor (not a volunteer) on many lab days including today, and `volunteer_events`/`volunteer_event_signups` don't exist in production at all — so the removed signal was the only thing ever setting the flag, and every regularly-assigned instructor was equally mislabeled, not just Rae. Removed the false signal; classification now relies solely on the real `volunteer_events`/`volunteer_event_signups` source. Display-only, no schema/data change. tsc/build/eslint clean.
- `0864abf` | **Self-service student WITHDRAW, separate from DELETE (Task Handoff Queue, Ben pre-authorized 7/9).** Investigation confirmed the withdraw-preserve DATA mechanism already worked (7 students `status='withdrawn'`, all retain `cohort_id`) and DELETE was already superadmin-only + guarded (`lib/deletion-requests.ts` queue, shipped earlier) — the gap was no dedicated withdraw UI action existed; instructors only had the scary Trash/Delete button (silently queues an approval request for non-superadmins) or a buried, ungated status dropdown inside the generic Edit form. Added `POST /api/students/[id]/withdraw` (lead_instructor+, mirrors `/graduate`/`/re-enroll`): sets `students.status='withdrawn'` without touching `cohort_id`, closes the active `student_program_enrollments` row (so `/re-enroll`'s insert doesn't collide with `one_active_enrollment_per_student`), writes a `student_cohort_history` row (`event_type='withdrawal'`). New `WithdrawModal` component + dedicated "Withdraw" button on the student detail page, next to Transfer/Mark Graduated, distinct from Delete. Also closed a permission gap in the generic student PATCH endpoint: any instructor role (not just lead_instructor+) could previously flip `status` via the plain Edit form, bypassing the intended model — now gated on lead_instructor+, but only when the value actually changes (so plain-instructor edits to notes/phone/scrub size keep working). No schema change, no data mutation to existing rows. tsc clean, build green, eslint diff-clean vs baseline.

## 2026-07-09

- `2456727` | **Fix: allow protected superadmins to self-tag `primary_program`.** Task Handoff Queue HOTFIX — Ben confirmed `bhartnell`/`jlomonaco` (both `role='superadmin'`, both `PROTECTED_SUPERADMINS`) couldn't be tagged from `/admin/users` because the Program-tag dropdown added in `573bc955` was gated by the same `canModify && !isProtected` condition as the Deactivate/Part-Time controls, hiding it unconditionally for protected rows. `PATCH /api/admin/users` already permits a protected superadmin to edit their *own* row (`isProtectedSuperadmin` only blocks other users' edits) — the UI just didn't mirror that. `app/admin/users/page.tsx` now also renders the Program dropdown when `currentUser.email` matches the row's `user.email`; Deactivate/Part-Time remain hidden for protected accounts (unchanged, out of scope). DB check found both accounts already `primary_program='paramedic'`/`is_part_time=false`/calendar-connected (tagged out-of-band before this fix landed), so this closes the UI gap for any future self-edit rather than a data fix. tsc 0 + clean build.
- `bc600429` | **Deploy RT/ACLS-help-only instructor exclusion (cherry-picked from draft PR #23).** Christopher Hooshmand, Tiffany Mate, and Denise Ridgell hold `lab_users` instructor rows for ACLS duty only (`primary_program` null, not paramedic-tagged) but were still showing in the paramedic lab-day "available instructors" list — the fix existed since `4b7fd18` on branch `claude/friendly-mayer-2mwvb8` but only ever shipped inside draft PR #23, never merged to `main`, so production never had it. Cherry-picked the isolated commit (`lib/rt-only-instructors.ts` name/email exclusion applied only to `GET /api/lab-management/instructor-availability`; the general `/api/lab-management/instructors` dropdown list is untouched so they stay assignable to ACLS stations). No records touched. Investigated as Task Handoff Queue item confirming Ben's live-prod report of Chris/Denise/Tiffany still appearing despite "RT-exclusion marked Done" — root cause was exactly that: done in a sandbox, never deployed. PR #23's other two fixes (general-lab-sync scope correction + LVFR `additional_instructors` wiring) are calendar-sync changes that still need Ben to run Sync All with production Google OAuth post-merge — left in the draft PR, not cherry-picked here, to avoid conflating an already-safe display fix with sync logic pending live verification. tsc clean, build clean, eslint clean.
- **Investigated, NOT a code bug — reported to Ben for a design decision.** The same ticket also reported Ben/Schafer/Josh (paramedic-tagged, full-time, full July `instructor_availability` coverage) as wrongly HIDDEN from the same list. Traced against live Supabase data: each has a real, currently-scheduled class block in `pmi_schedule_blocks` that overlaps the 08:00–17:00 lab window on the date Ben tested (Ben: EMS 182 Pediatrics 10:00–12:00; Josh: EMS 192 Trauma 13:00–14:30; Schafer: EMS 172 Medical Emergencies 08:30–10:00). `app/api/lab-management/instructor-availability/route.ts`'s conflict check (`timesOverlap`) correctly flags ANY overlap — even a 1.5hr class inside an 8.5hr window — as a full-day conflict, which is existing, deliberate behavior ("conflict trumps everything," `6929e14`) and also gates the same signal used by `EditStationModal`/`LabDayRolesSection` to prevent double-booking an instructor into a lab station during a class they're teaching. This is NOT the "inverted boolean" the ticket hypothesized — it is the system working as designed on real data. Loosening it (e.g. so a full-timer with a short class still shows "available" for the rest of the day) is a scheduling-safety design change, not a display bug, so it was left for Ben rather than changed autonomously. See Task Handoff Queue item and Session Log for the full write-up and two candidate fix options.

## 2026-07-08

- `c338660` | **LVFR AEMT — blank printable skills class checklist (Task Handoff Queue item).** Verified what already existed on `/lvfr-aemt/skills` Class view first: "Completion sheets (.zip)" is per-student and name-stamped (not blank), and "Reference sheets (.pdf)" is a detailed 30-page document (one page per skill, for teaching/hand-documenting a failure) — neither is the compact blank checklist Ben asked for. Added `lib/reports/lvfr-skills/blankChecklist.ts` + `GET /api/lvfr-aemt/skills/blank-checklist-export` (instructor+): one print-friendly document, all 30 skills grouped by their 7 categories, empty checkbox + date line per skill, blank Class/Date/Instructor fields, instructor sign-off. Wired a third "Blank checklist (.pdf)" export button on the Class view next to the other two. Additive only, no schema/data changes. tsc 0 + clean build.
- `1ebe87f` | **Lab day: label conflict/no-data instructors, pair panel with skill coverage.** `AvailableInstructorsSection.tsx` previously only rendered Available/Volunteering groups and silently dropped conflict/no-availability-data instructors instead of labeling them — fixed per Task Handoff Queue request ("don't show bare 'unavailable'"): adds Conflict (amber, with the conflicting event as the reason) and No availability submitted (gray) rows, using data the classification endpoint already returned. Also moved the panel into the existing xl+ floating aside alongside `SkillCoveragePanel` (the page's spare screen space), mirroring the established skill-coverage responsive pattern; no change below the xl breakpoint, so tablet-landscape is unaffected. A companion queue item requesting SMC auto-tracking + mobile compression for the skill-coverage panel was closed as already-implemented (verified against `SkillCoveragePanel.tsx` + `smc-completion` route — no code change needed). tsc 0 + clean build.
- `2fc1f682` | **General-lab-default, Part 2 — the labs-not-showing FIX (Option B + precedence).** Full-time paramedic-tagged instructors now get a general-lab calendar event on every upcoming paramedic-cohort lab day, synced to Google `primary` like classes — the missing mechanism the diagnosis identified (labs only generated from station/role assignments, which Ben had on 0 of 23 upcoming lab days). New `general_lab` source_type (migration `20260708_gcal_general_lab_source_type.sql`, constraint widened before any sync per the ship-with-migration rule); `syncGeneralLabDefault`/`removeGeneralLabDefault` in `lib/google-calendar.ts` (idempotent check-before-create + compensating-delete, `source_id = lab_day_id`, Sage color to distinguish from station/role). **Precedence** (`lib/general-lab-sync.ts`, run as step 6 of the admin "Sync All"): a didactic CLASS that date OVERRIDES → a STATION/ROLE assignment REPLACES → else the general-lab-default is created; the pass also removes any general-lab event a now-more-specific assignment supersedes, so re-running Sync All converges. **Scope = Option B** (Ben): all `primary_program='paramedic'` + `is_part_time=false` + calendar-connected instructors get all paramedic labs (fails safe toward showing every lab). Stacie (EMT-tagged) is never generated for — her calendar feeds admissions and must not be over-blocked (memory `stacie-calendar-admissions-constraint`). Also fixed the residual fire-and-forget on the `lab_day_roles` DELETE handler (`removeLabDayRole` now awaited, both single + bulk). **Ben:** tag the paramedic instructors in `/admin/users`, then click Sync All on `/admin/calendar-sync` → labs populate. tsc 0 + clean build.

- `573bc955` | **General-lab-default, Part 1 — instructor program tag (`lab_users.primary_program`).** First increment of the launch-critical "labs not showing on Ben's calendar" fix (diagnosis confirmed the root cause is the absence of a general-lab event type — labs only generate from station/role assignments, and Ben has none on any of the 23 upcoming lab days). New nullable `primary_program` column (`'paramedic' | 'aemt' | 'emt'`, CHECK constraint; migration `20260708_lab_users_primary_program.sql`, additive/idempotent) — combined with the existing `is_part_time` flag it identifies "full-time paramedic instructor" (the set that will get the general-lab-default event). Wired through `/api/admin/users` (select + PATCH) and a per-instructor program-tag dropdown in the admin user editor (`app/admin/users/page.tsx`); `User` type + `docs/DATABASE_SCHEMA.md` updated. Ben sets the tags. Part 2 (the general-lab-default sync + class>station>general-lab precedence, emitted through the same Google path classes use) is the next increment. tsc 0 + clean build.

- `6929e14` | **Fix: full-time instructors with no conflict wrongly excluded from lab-day instructor dropdowns.** Root cause was NOT an inverted boolean — it was default-unavailable logic: `app/api/lab-management/instructor-availability/route.ts`'s group-classification block required `has_explicit_availability` (an `instructor_availability` row) to reach the "available" group, but full-time instructors never submit those rows (that's the part-timer opt-in flow), so a conflict-free full-timer fell through to `"no_availability"` and was hidden by `isDefaultVisibleGroup()` in the `EditStationModal`/`LabDayRolesSection` pickers. Fix: full-time instructors (`!is_part_time`) now default to `"available"` unless an actual conflict was found (conflict check still runs first and wins); part-timers unchanged (still require explicit availability); volunteer precedence and the "Show all instructors" override toggle untouched. `is_open_lab_instructor` (unrelated Wednesday student-signup feature) not touched. tsc 0 + clean build.

- `3d15514` | **Data-safety fix: stop Force re-sync deleting unrecoverable lab calendar events; await lab-day-role calendar sync.** Investigation (Task Handoff Queue) found Force re-sync (`app/api/admin/calendar-sync/force-resync/route.ts`) deleted ALL of a user's `google_calendar_events` rows with no `source_type` filter, but only recreates `schedule_block`/`schedule_block_series` events — `station_assignment`/`lab_day_role` events were being permanently deleted with no recreate path. Scoped both the fetch and delete queries to `RESYNCABLE_SOURCE_TYPES = ['schedule_block', 'schedule_block_series']` so lab-sourced mappings/events are left untouched until a real lab-recreate path exists. Also fixed `app/api/lab-management/lab-day-roles/route.ts`'s POST handler, which called `syncLabDayRole(...).catch(() => {})` without `await` — same fire-and-forget bug class already fixed elsewhere in `0a98e539` (Vercel freezes the serverless function on response, killing detached syncs), now awaited and logged on failure. The DELETE handler in the same file has an identical fire-and-forget pattern (`removeLabDayRole`) — flagged as a follow-up, not fixed here (out of scope for this fix). tsc 0 + clean build.

- `a430779` | **Fix two Ben-filed feedback reports: reseed accidental-click hazard + LVFR cohort display name.** (1) `b704db91`: hardened the "Re-seed Today's Runsheet" admin control on `/lvfr-aemt` (`app/lvfr-aemt/page.tsx`) — replaced the inline button-swap confirm (which put "Confirm" at the same screen position as the trigger, an accidental-click risk even though the button is already admin-gated + separated from the day view per `c800adc`) with a real modal (`ReseedConfirmModal`) requiring the admin to type `RESEED` to arm the destructive action, and moved the trigger into a visually distinct "Danger Zone" sub-block. (2) `dffcf1bb`: the imported LVFR AEMT cohort (`7aa0d46`) displayed as bare cohort_number `"1"` on `/lvfr-aemt/skills`. Added nullable `cohorts.display_name` (migration `20260708_cohorts_display_name.sql`, additive `IF NOT EXISTS`) and set it to `"LVFR AEMT Group 2"` for that one cohort row (single-row UPDATE by PK); `lib/lvfr-cohorts.ts`, `app/lvfr-aemt/skills/page.tsx`, and `app/api/lvfr-aemt/skills/completion-export/route.ts` now prefer `display_name`, falling back to `Cohort {cohort_number}` when NULL — PMI's own cohorts are unaffected (column stays NULL for them). Also corrected `docs/DATABASE_SCHEMA.md`'s `cohorts.cohort_number` type (doc said `integer`, live is `numeric`) per the Schema-First Rule. Both `feedback_reports` rows flipped to `status='resolved'` with `resolution_notes`. tsc 0 + clean build.

- `09642814` | **ACLS/PALS timer bug cluster (pre-7/16 PALS course).** Verified 6 `feedback_reports` timer bugs individually against current code; 2 were already fixed (GlobalTimerBanner realtime discovery + adv-cert/grade session-refresh guards, commits 2bb27bc/1284e38) and flipped to resolved with no re-fix. 4 genuine still-open bugs fixed: (1) `ef5f2975` timer-discovery latency on `components/LabTimer.tsx` + both `timer-display` pages — added the same Realtime subscription pattern already proven on GlobalTimerBanner/TimerBanner. (2) `d3639b05`/`1a31456e` rotation-duration edits on the Lab Day edit page never propagated to an already-existing `lab_timer_state` row — `app/api/lab-management/lab-days/[id]/route.ts` PATCH now mirrors the new duration into the timer row (only when `status='stopped'`) and into `lab_stations.rotation_minutes`; also fixed `LabTimer.tsx`'s progress bar/footer, which read the static `rotationMinutes` prop instead of live `duration_seconds`. (3) `33bc4965` O2/NRB station showed a wrong 15-min duration and no prep timer — `components/NremtTimer.tsx` now sources duration + prep/eval phases from `lib/nremt-instructions.ts` (the same authoritative data already used correctly by `NremtCandidateInstructions.tsx`) instead of its own separate/wrong hardcoded map + universal 4-phase O2+BVM combo. Architecture kept as-is (already server-authoritative: single `lab_timer_state` row, client-interpolated) — targeted fixes judged lower-risk than a rebuild this close to the 7/16-17 PALS course, which reuses this same machinery unmodified. All 6 `feedback_reports` rows flipped to `resolved` with notes. tsc 0 + clean build.

- `cf769364` | **LVFR follow-ups (Ben-approved): un-flag PMI's AEMT 2.0 + filter LVFR students from PMI-facing lists.** (1) **Data fix:** cohort `6796e139` (PMI's real internal "AEMT 2.0", 10 `@my.pmi.edu` students Ben personally recognized) was wrongly `is_external_program=true` — reverted to `false` (single-row UPDATE by PK; current row snapshotted to `_backup_cohort_6796e139_20260708` first). Its dates are also inconsistent (start_date `2026-07-14` = the LVFR launch date, clearly copied during the earlier mis-repurposing; expected_end `2026-05-02`; end_date `2026-09-17`) — **NOT changed**, flagged to Ben: the cohort was created 2026-01-13 with students added ~2026-01-20, so it's a January-start cohort and the `7/14` start is the corrupted value. Ben confirms real term dates. (2) **Roster-leak filter:** `/api/students`, `/api/lab-management/cohorts`, and `/api/cohorts` now exclude `is_external_program=true` cohorts (LVFR) from PMI-facing lists/pickers so they don't clutter PMI views for inspection. The two cohort endpoints gained an `include_external=true` opt-in (mirrors the existing `include_archived` pattern); the cohort manager (`app/academics/cohorts/page.tsx`) passes it so LVFR stays manageable there. `/api/students` filters external-cohort students in-app while keeping unassigned (null-cohort) students. LVFR skills tools unaffected (they select by `programs.abbreviation='LVFR'`). tsc 0 + clean build.

- `7aa0d469` | **LVFR AEMT student import + fix silently-broken LVFR skills cohort selector.** Imported Ben's 21 LVFR (Las Vegas Fire Rescue) AEMT students for the 7/14 launch under a NEW dedicated `'LVFR'` program + cohort (`program 691df113…`, `cohort b67958a0…`, `is_external_program=true`, `current_semester=null`) — deliberately NOT PMI's internal AEMT 2.0 (`6796e139`). The dedicated program means PMI's program-**allowlist** views (clinical tracker/readiness `PM/PMD`, clinical hub phases, EMT/AEMT tracking, internships) exclude LVFR automatically with zero code changes. **Bug found + fixed:** all LVFR per-cohort skills readers selected cohorts via `cohort_number.ilike '%LVFR%/%AEMT%'`, but `cohort_number` is NUMERIC — `numeric ILIKE text` throws and the callers swallowed the error (`data || []`), so the LVFR skills class-completion / matrix / summary endpoints had been silently returning ZERO cohorts (consistent with 0 rows in every LVFR skill table). New shared `lib/lvfr-cohorts.ts` (`getLvfrCohorts`/`getLvfrCohortIds`) selects by `programs.abbreviation='LVFR'`; the 3 skills routes now import it (one source, can't drift). Verified: the fixed selector returns the LVFR cohort with all 21 students; PMI allowlists exclude LVFR; `6796e139` untouched. **Flagged separately (NOT changed):** (1) `6796e139` is mis-flagged `is_external_program=true` with `start_date=7/14` — it's PMI's real internal AEMT (10 `@my.pmi.edu` students) and needs un-flagging (escalated, Ben's call); (2) the same numeric-ILIKE bug still affects `lvfr-aemt/dashboard` + `grades/import` + `grades/summary` (out of scope here — grades explicitly not needed); (3) show-all endpoints (`/api/students`, `/api/lab-management/cohorts`, `/api/cohorts`) have no program filter, so the LVFR cohort/students still surface in the general roster + cohort dropdowns (not in any clinical/report/phase computation). tsc 0 + clean build.

## 2026-07-07

- `b6f9d68` | **Clinical Tracker: EMT shows neither Complio nor mCE as applicable.** Ben-approved decision resolving the item flagged unresolved in `862a8e5`. `app/clinical/clinical-tracker/page.tsx`: EMT cohorts now disable BOTH the Complio and mCE tabs (mirrors the AEMT N/A pattern, which only disabled Complio) and the aggregation panel + student table are replaced with an explanatory N/A state — "Complio and mCE do not apply to EMT students... tracked on the EMT Tracking page" (links to `/clinical/emt-tracking`) — rather than silently showing empty/misleading data. `tracker-readiness` was already scoped to PM/PMD cohorts only (from `862a8e5`), so no "behind" notification changes were needed. No corresponding `feedback_reports` row (task originated as a Claude-flagged decision item, not a user report). Source: Task Handoff Queue. tsc 0 + clean build + eslint clean on touched file.

- `04833de6` | **Seam-1 tidy-up: extract shared lab-day dedup logic.** The unified calendar API (`app/api/calendar/unified/route.ts`) and the public ICS feed (`app/api/calendar/feed.ics/route.ts`) each reimplemented identical lab_day-vs-schedule-block dedup logic (explicit FK + date/cohort/section fallback key) as parallel copies — the one place a future edit to one copy but not the other could reintroduce drift. Extracted into new `lib/calendar-lab-dedup.ts` (`createLabDedupTracker`/`trackScheduleBlockForLabDedup`/`isLabDayDeduped`), both routes now import it. Pure refactor, no behavior change — the unified API's extra "hide monolithic section-1 fallback when sectioned rows exist" logic (not present in the ICS feed) was left untouched since it isn't part of the duplicated code. Source: Task Handoff Queue low-priority tidy-up item. tsc 0 + clean build + eslint clean on touched files.

## 2026-07-06

- `5b92a868` | **(Superseded duplicate — reconciled in the merge above.)** This session and a parallel session independently implemented the same seam-1 dedup extraction within the same minutes, despite a claim note on the board task. The parallel session's version (`04833de6`, `lib/calendar-lab-dedup.ts`) landed on main first via PR #16 and was kept verbatim in the merge resolution; this commit's functionally-identical `lib/calendar-dedup.ts` was removed in the merge. No behavioral difference existed between the two. Second occurrence of the parallel-duplicate pattern (see the `d2df3910` SignaturePad precedent).

- `d3fe3481` | **Feedback fixes + sortable internship tracker + 2 features verified already built.** **Bug 1 (Stacie, aa519cdf):** the Skill Coverage panel re-opened itself after every save on the lab-day page (the post-save refetch remounts the panel, resetting to `defaultExpanded`) — expanded/collapsed state now persists in `localStorage` (`components/lab-day/SkillCoveragePanel.tsx`). **Bug 2 (Rae, 8ac4ace6):** removed Attestation, Docs Shared w/ Sites, CHH Receipt, and CHH Approval from the STUDENT print-out only (`ComplioStudentPrintView` in `app/clinical/clinical-tracker/page.tsx`) — all four remain in the admin tracker table. **Verified already built + flipped (264b0f44):** the LVFR daily schedule is in the runsheet (3-tier rework: session time headers, per-item time labels, brief/debrief). **Feature (93592567): sortable/filterable internship tracker** — `/clinical/internships/cohort/[id]` phase-overview table now has clickable column-header sorting (Student, Phase, Phase started, FTO, Agency, NREMT; asc/desc with indicators) plus a text filter matching student/FTO/agency. Pure presentation layer per the spec's read-only guardrail — no query, data, or structural changes. **Features verified ALREADY BUILT, no work needed (flipped): (0417c52c)** station doc/PDF import — full multi-doc upload system already exists (EditStationModal upload/link modes → `/api/lab-management/stations/[id]/documents` → Supabase Storage bucket `station-documents`, PDF/DOCX/images, 10MB cap, delete/rename, URL option retained); **(ecc6394c)** flipped seating chart — `/labs/seating/charts/[id]` already has the Instructor/Student view flip toggle (row + seat mirroring) and a landscape print stylesheet. All 6 feedback rows flipped with resolution notes. tsc 0 + clean build.

- `083652b7` | **CLAUDE.md: Data Safety standing order (Ben, high priority).** New HARD REQUIREMENT section near the autonomy/claim-first rules: fix data-safety/vulnerability issues ON SIGHT when safely in-scope (not note-for-later); the failure mode to prevent is silent/irreversible data-content deletion (relogs/reconnects tolerable, data loss not — favor log+abort over destroy-on-failure); canary/pre-check before bulk production operations; existing guards (--backup, no autonomous hard-delete, reversible paths) stay. Deep audit deliberately out of scope for now. Motivated by the 2026-07-06 token-wipe incident. Docs-only.

- `ba8659ee` | **INCIDENT + fix: token-refresh failure handling wiped 8 instructor calendar connections; refresh now distinguishes revoked-token from bad-client-credentials.** During the approved backfill (step 6 of the calendar patch plan), the backfill script ran locally with `.env.local`'s **pre-rotation** `GOOGLE_CLIENT_SECRET` — Google rejected every token refresh with `invalid_client`, and `getAccessTokenForUser` treated each failure as a revoked token, clearing all 8 connected instructors' `google_refresh_token`/`google_calendar_connected` in production. **No calendar events were created or deleted** (no valid token was ever obtained; instructors' actual Google Calendars are untouched; mapping table unchanged at 33 rows). Recovery = restore the 8 rows from the Supabase daily backup OR the 8 instructors reconnect via Settings (Ben's call — flagged). **Fixes:** new `refreshAccessTokenWithReason()` in `lib/calendar-availability.ts` parses Google's OAuth error; `getAccessTokenForUser` now clears the stored connection ONLY on `invalid_grant` (token genuinely dead) — `invalid_client` (our credentials wrong) and network errors log + return null without destroying the user's token. `scripts/backfill-calendar-sync.ts` (new, committed for reuse) now canary-tests one token refresh and hard-aborts on `invalid_client` before touching any user. tsc 0 + clean build.

- `0a98e539` | **Calendar sync launch-blocker fix (Ben-approved): LVFR CHECK-constraint + idempotency hardening + station-assignment sync wiring.** Investigation (logged on the Notion task) found the May 6 "4 duplicate events" incident's root cause — a `source_type` value missing from `google_calendar_events`'s CHECK constraint, silently rejecting mapping rows so the idempotency lookup always missed — had been **reintroduced verbatim** by the 2026-06-30 LVFR sync (`c4700ba`): `syncLvfrAssignment` writes `source_type='lvfr_assignment'`, never added to the constraint. **Migration** `20260706_widen_gcal_source_type_lvfr.sql` (applied with `--backup=google_calendar_events`, 33 rows snapshotted at `_backup_google_calendar_events_20260706165641`) widens the constraint. **Idempotency guarantees:** `syncLvfrAssignment` already checked-before-create; it now also **deletes the just-created Google event if the mapping save fails** (event ⟺ mapping can never diverge, so constraint drift can never cause duplicates again); `syncLabStationAssignment` gained the same compensating delete PLUS a check-before-create it never had (two calls previously meant two events, with the mapping upsert orphaning the first). **Orphaned-event cleanup: zero orphans exist, proven by data** — all 30 `lvfr_aemt_instructor_assignments` rows have NULL primary AND secondary instructors, so `syncLvfrAssignment` never ran with a token and never created an event. **Fire-and-forget fixes (why station assignments synced zero events ever despite 180 assignments):** Vercel freezes the serverless function when the response returns, killing detached sync promises — `station-instructors` POST and the LVFR `scheduling` PUT hooks are now awaited; `stations/[id]` PATCH (which never called the sync at all) now syncs the newly assigned primary instructor, awaited, safe against the dual-route EditStationModal flow via the new idempotency check. **"Preference-key mismatch" verified NOT to exist** — settings UI, `/api/settings/calendar-sync`, and `shouldSyncForUser` all use the same `preferences.calendar_sync.sync_lab_assignments` path (and all 8 connected instructors have null prefs = default-enabled). `CALENDAR_SYNC_CRON_ENABLED` re-enable left to Ben (Vercel env). `docs/DATABASE_SCHEMA.md` constraint entry updated with the ship-with-migration rule. tsc 0 + clean build.

## 2026-07-02

- `d334c0c` | **Suppress clinical-hours push notifications, keep overview intact.** Notion Task Handoff Queue item (CLINICAL-HOURS false-flag). Ben's confirmed diagnosis: `/clinical/hours` is a manual/convenience Platinum-export tracker, not the primary system, and shouldn't push "behind on clinical hours" notifications. Traced the notification origin to `app/api/cron/clinical-hours-reminder/route.ts` (weekly Monday cron) — both the per-student reminder and per-cohort instructor alert `createNotification` calls. Gated both behind a `NOTIFICATIONS_ENABLED = false` flag; the cron still runs and logs a summary but no longer writes to `user_notifications`. The `/clinical/hours` overview's own "Behind" pace badges (`getStudentPace`) are untouched. tsc 0 + clean build.
- `b2f77cc` | **Go-live: wire weekly feedback-digest cron, per-reporter, skip-empty-weeks.** Notion Task Handoff Queue item (FEEDBACK DIGEST). Ben approved both open parameters (weekly cadence, only-when-resolved-items; per-reporter not team-wide scope) — the route already implemented both, just needed wiring. Added `/api/cron/feedback-digest` to `vercel.json` (Sun 7am UTC), refreshed its stale "not yet wired" docstring, and corrected `docs/DATABASE_SCHEMA.md`'s stale "migration pending" note for `feedback_reports.digest_sent_at` (confirmed live in prod via direct query — migration `20260701_feedback_reports_digest_sent_at.sql` and its index are applied). Verified grouping via an equivalent direct-SQL dry run against prod (no local CRON_SECRET/Supabase creds in this session): 3 reporters, 13 resolved items, correctly grouped per-reporter with `resolution_notes` present where set. tsc 0 + clean build.
- `e370c2e6` | **Bug cleanup + standing feedback-status workflow.** Verified 11 `feedback_reports` rows marked `'new'` were already fixed in code (PRs #8-14): tab-switch grading wipe (8910bda1), lab-groups move-dup (0f6af46a), equipment-save 500 (03d951f9), calendar 502 + costs 500 (9aad3705), grading send-email nav (e9d8c915), clinical-hours S1/S2 false-flag (801e44c7), G12 internship dup (71f25b19), deletion-requests queue gap (cd5ac5ee), exam-session edit (a3ff7e71) — flipped all 9 to `status='resolved'` with a `resolution_notes` pointing at the fix. One (`abc42377`, the "Google Sign-In: Internal Mode" admin banner) turned out to be informational, not a code bug — flipped to `'archived'` instead of falsely claiming a fix. **3 genuine bugs fixed:** (1) `73d4dcd6` lab groups weren't rendered in numeric order — added `.sort((a,b) => a.group_number - b.group_number)` in `app/academics/cohorts/[id]/groups/page.tsx`; (2) `ca8caf18` LVFR quiz-review items showed an "optional" tag when they're mandatory — `app/lvfr-aemt/day/[date]/page.tsx` now treats `item_type==='quiz'` as always-required in both the progress meter and the badge display, regardless of the stored `requirement` value (12 legacy DB rows still say `'optional'`; a data-level bulk UPDATE was attempted, correctly blocked by the safety classifier as an unescalated production `UPDATE`-many per CLAUDE.md's Bucket 2, so a display-layer fix was used instead — flagging the legacy rows to Ben separately); (3) `16312df5` the Skill Coverage / SMC checklist panel lived on the lab-day *edit* page (where stations are configured, not reviewed) — moved `SkillCoveragePanel` to the main lab-day page `app/labs/schedule/[id]/page.tsx`, removed from `app/labs/schedule/[id]/edit/page.tsx`. Also verified `95a56f77` (LVFR morning/afternoon ordering) was already fixed by the earlier 3-tier runsheet rework — flipped to resolved, no re-fix needed. **New standing rule in `CLAUDE.md`:** when Code fixes a bug tied to a `feedback_reports` row, flipping that row's status to `resolved`/`archived` + `resolution_notes` is now part of the fix itself, not a follow-up step; only bugs needing clarification or with conflicting reports get escalated to Ben. `docs/DATABASE_SCHEMA.md` annotated. tsc 0 + clean build.

- `862a8e5` | **Clinical Tracker: full program labels on the cohort dropdown, AEMT mCE-only tabs, EMT/AEMT excluded from tracker-readiness notifications.** Notion task (Ben-approved decisions, implementation only — not open for redesign). (1) `app/clinical/clinical-tracker/page.tsx`: the cohort `<select>` embedded `program:programs(id,name,abbreviation)` from `/api/lab-management/cohorts?activeOnly=true` (already returned by the API, just unused) but rendered bare `cohort.name || Cohort ${cohort_number}`. New `cohortLabel()` helper renders `${abbreviation} G${cohort_number}` (e.g. "PM G14", "AEMT G2"), matching the convention already used in ~15 other cohort selectors app-wide (`site-visits`, `request-coverage`, `lab-day-roles`); falls back to the old bare format if `program` is missing on a row. All cohorts are still kept in the dropdown — this is a label-only change, no data/query change. (2) Same page: AEMT cohorts now disable the Complio tab (shown as "Complio (N/A)", not clickable) and auto-switch to the mCE tab when an AEMT cohort is selected — Complio isn't applicable to AEMT students, driven off `cohort.program.abbreviation`. Paramedic keeps both tabs unchanged. (3) `app/api/clinical/tracker-readiness/route.ts`: this endpoint (feeding the "Clinical Tracker readiness" ready/total chips on `/clinical/overview` and `/clinical/page.tsx`) now joins `programs.abbreviation` and filters to PM/PMD cohorts only, excluding EMT/AEMT from the rollup even if they happen to have `student_compliance_docs` rows — matches the `pmCohorts` filter `/clinical/page.tsx` already applied client-side for the same data, but `/clinical/overview/page.tsx` was NOT filtering by program and could surface an EMT/AEMT "behind" chip under the Paramedic-labeled "S2 - Compliance" phase; fixing it at the API source covers every consumer. `app/api/clinical/overview-all/route.ts` checked — it already keeps EMT/AEMT clinical-status branches on separate tables (`emt_student_tracking`/`aemt_student_tracking`) and pushes no alerts off `student_compliance_docs` for those programs, so no change needed there. **EMT tab-applicability (Complio/mCE) left unresolved and flagged for Ben**, per the task's own instruction not to guess: `app/clinical/emt-tracking/page.tsx` has a comment "MCE checkbox removed per Ryan's feedback — EMT students do NOT have mCE," but that's about the separate `emt_student_tracking` table/page, not this Complio/mCE tracker built on `student_compliance_docs` — suggestive, not conclusive for this page. EMT cohorts keep both tabs shown (like Paramedic) pending Ben's call. `docs/COMPONENTS.md` not touched (page-local change, no shared component). tsc 0 + clean build.

- `d3a52af` | **Instructor availability on lab days — map-first UI wiring.** Ben-approved (map-first approach), wires the existing `instructor_availability` classification endpoint (`GET /api/lab-management/instructor-availability`) into two more surfaces that previously only had the unrelated Google-Calendar `calendarAvailability` free/busy indicator: `EditStationModal.tsx`'s "Add instructor" picker and `LabDayRolesSection.tsx`'s role-assignment picker now default to `available`/`volunteer` instructors (green/blue), with a "Show all instructors" toggle to reveal `conflict`/`no_availability` instructors — never hard-blocks picking someone. New `AvailableInstructorsSection.tsx` — an at-a-glance "Available Today" panel on the lab day page (`app/labs/schedule/[id]/page.tsx`), fetched once at the lab-day level (08:00-17:00 window, matching the existing `stations/new` convention) and shared by all three consumers, not refetched per station. New shared helper `components/lab-day/instructorAvailability.ts` + `InstructorAvailabilityEntry` type in `components/lab-day/types.ts`. Desktop-first two-column layout (`grid-cols-2 max-md:grid-cols-1`) per the UI Layout Rule. Read-only against the availability data model — no schema change, no migration. `docs/COMPONENTS.md` updated. tsc 0 + clean build.

## 2026-07-01

- `937246c7` | **Clinical overview redesign — phase-segmented hub, GRADUATED cohort status, S4 internship rollup, mobile Hospital Visit Log.** Ben-approved, full-spec build (Notion `[CLINICAL OVERVIEW - APPROVED, ready for Code]`). **Schema** (`20260701_cohorts_status_graduated.sql`, `--backup=cohorts`, 10 rows snapshotted): new `cohorts.status` (`'active' | 'graduated'`, default `'active'`, CHECK constraint) — cohort-level, distinct from the existing per-student `students.status='graduated'` system. `app/api/lab-management/cohorts/[id]/route.ts`'s PATCH handler already whitelisted `status` (dead code until now, column didn't exist) — zero API changes needed; all cohort-read routes use `SELECT *` so `status` flows through automatically. **`app/academics/cohorts/page.tsx`** (Cohort Manager): new `handleToggleGraduated` + a Graduate/Un-graduate action button + a "Graduated" badge, mirroring the existing Active/Archived toggle pattern exactly. **`app/clinical/page.tsx`** (the hub) — full redesign: (1) removed the old raw-counts "Quick Stats" block (preceptors/sites/agencies/in-internship totals); (2) phase-segmented S1-S4 strip now always renders all 4 tiles (previously skipped empty phases) with an explicit placeholder when a phase has 0 cohorts; (3) S4 tile shows a read-only internship rollup (phase-1/phase-2 counts + at-risk badge) grouped client-side from the already-fetched `/api/clinical/internships` — no new write path, no rebuild of the internship tracker; S4 cohort links now correctly point at the existing `/clinical/internships/cohort/[id]` cohort-view page (was silently landing on the unfiltered list page via a `?cohortId=` param that page never read); (4) new collapsed-by-default Graduated section listing `cohorts.status='graduated'` cohorts, still clickable into Clinical Tracker/Internships/Hours; (5) new "Log a Hospital Visit" quick-access banner at the top of the page — the one field/phone exception to desktop-first per `CLAUDE.md`'s UI Layout Rule, linking to the existing `/clinical/site-visits` page (kept desktop-first as-is; a full mobile rebuild of its filters/export UI was judged disproportionate to this one bullet). EMT/AEMT null-semester cohorts correctly stay excluded from the S1-4 paramedic-only phase view (unchanged). `docs/DATABASE_SCHEMA.md` + `docs/SITEMAP.md` updated. tsc 0 + clean build.

- `(pending)` | **CLAUDE.md: claim-first discipline for the Notion board.** New HARD REQUIREMENT rule, in response to today's parallel-session collisions (the dropdown bug fixed two ways, the signature-clear bug fixed identically twice by separate sessions, requiring a merge-conflict resolution). Before claiming a Queued board item: `git pull` / check for recent merges first (another session may have already fixed it — mark Done with a note instead of re-fixing), then set Status to `In Progress` immediately, before investigating or coding — that write is the claim, letting a parallel session skip the item instead of racing it. Companion to the prompt-precedence rule above. Docs-only, no code change.
- `702a7b94` | **Clinical Tracker cohort dropdown fix + SignaturePad clear fix** (both later found to be independently duplicated by a parallel session — see the `d2df3910` merge entry below for how they were reconciled). Fixed the dropdown by pointing the client at the established `/api/lab-management/cohorts?activeOnly=true` endpoint (used by 10+ other pages) instead of the nonexistent `/api/cohorts`; improved the default-cohort selection to prefer one with `student_count > 0`. Fixed SignaturePad's clear by reassigning `canvas.width` instead of `setTransform`+`clearRect`, per Ben's repro and diagnosis (dpr backing-store mismatch). tsc 0 + clean build.
- `0833bf3f` | **fix(grading): TimerBanner realtime discovery for the grading-page timer, closing the gap PR #9 missed.** PR #9 added a Supabase Realtime subscription to `GlobalTimerBanner.tsx` for instant timer-started discovery, but `GlobalTimerBanner` is deliberately disabled on `/labs/grade/*` pages — `TimerBanner.tsx` does its own discovery there via a documented 30s poll (a known gap from a 2026-05-28 fix) that PR #9 never touched. Applied the same additive Realtime pattern directly to `TimerBanner`, filtered to `lab_day_id`; no new migration (reuses PR #9's RLS policy + publication membership for `lab_timer_state`). Investigated Ben's "timer only shows if ready is checked" hypothesis first — not confirmed in code; `TimerBanner` mounts unconditionally given a valid lab day, `isReady` only drives the internal ready-toggle button. tsc 0 + clean build; deploy verified green.
- `d2df3910` | **Merge: resolved a real conflict in `components/SignaturePad.tsx`** between two independently-authored, functionally-identical fixes for the same signature-clear bug (this session's `702a7b94` and the parallel session's `6b1ab36f`, below) — both reassign `canvas.width` to force a full backing-store reset instead of `setTransform`+`clearRect`, differing only in helper-function naming. Kept the already-live naming (`configureCanvas`) verbatim rather than re-diverging. Also merged this session's Clinical Tracker cohort-dropdown fix (`702a7b94`, client now calls the established `/api/lab-management/cohorts` endpoint) alongside the parallel session's independent fix for the same bug (`e254eff2`, below — creates the missing `/api/cohorts` route instead); both fixes coexist without conflict since one changed the client, the other the server. tsc 0 + clean build on the fully-merged state; deploy verified green.
- `6b1ab36f` | **Signature-pad clear, take 2 — canvas.width reset instead of setTransform+clearRect.** Ben pushed back on the earlier fix (`cafb554`, verified as "no defect" earlier today) with a concrete repro: clearing only wipes a smaller rectangle, leaving part of the hand-drawn signature on screen. Wrote a standalone Playwright/Chromium test replicating `clearRect`-after-`setTransform(identity)` at DPR 1/1.5/2/2.5/3 — empirically 0 leftover non-white pixels every time, so the existing approach is provably correct in Chromium. Couldn't test WebKit/Safari in this sandbox (no WebKit binary available), and PMI's stated usage pattern is heavily iPad — canvas transform+DPR-scaling quirks specific to WebKit are a known category of browser bug this environment can't rule out. Applied Ben's suggested alternative as a more robust cross-browser fix regardless: `components/SignaturePad.tsx` now clears by reassigning `canvas.width` (which fully discards and recreates the backing store, guaranteed to wipe 100% regardless of any transform-related browser quirk) instead of `setTransform`+`clearRect`, re-applying stroke style afterward via a shared `configureCanvas()` used by both the mount effect and `clear()`. tsc clean, build green, eslint clean.
- `e254eff2` | **Clinical Tracker cohort dropdown fix (missing `/api/cohorts` route) + `/clinical/overview` current_semester bug + phase-segmented readiness on the `/clinical` hub.** Notion Task Handoff Queue items (HIGH-BUG clinical tracker, CLINICAL OVERVIEW). (1) `app/clinical/clinical-tracker/page.tsx` fetches `GET /api/cohorts` to populate its cohort selector, but that route never existed — the dropdown always rendered empty, so no cohort could ever be selected and the page permanently showed "0 students". New `app/api/cohorts/route.ts` (mirrors `/api/lab-management/cohorts`'s query shape: `*, program:programs(id,name,abbreviation), student_count`) fixes this and also fixes `/admin/bulk-operations`, which called the same missing route. Verified: `students` table has no `is_active` column (only `cohorts` does) and the existing `/api/clinical/compliance` + `/api/clinical/mce-docs` routes already correctly filter on `status='active'` — no bug there. (2) `app/api/clinical/overview-all/route.ts`: the Supabase query selected `cohorts.semester` but the response mapper read `cohort?.current_semester` (a different, unselected field) — always `null`, silently breaking the S1-S4 phase grouping on `/clinical/overview` (`PM_SEMESTER_CONFIG` is keyed 1-4 off `current_semester`). Added `current_semester` to the select. Total-student counts were not themselves bugged (145 active students confirmed live in DB; no `!inner` join to drop rows) — the reported "0 Total Students" wasn't reproducible from static analysis or DB checks, may have been a transient session/auth issue when observed. (3) `app/clinical/overview/page.tsx`: removed the "Total/EMT/AEMT/Paramedic" program-type summary header — it duplicated the phase-segmented per-program sections below with a less useful grouping and was reading the broken field from (2); the phase sections are the source of truth. (4) `app/clinical/page.tsx` (the `/clinical` hub): added a "Clinical readiness by phase" section above the existing workflow cards — S1-S4 cohorts from the new `/api/cohorts`, cross-referenced with `/api/clinical/tracker-readiness` (already live) for S1/S2 ready/total badges; S3/S4 (no per-student readiness signal, and no S3 cohorts currently exist) show cohort + active-student counts, each linking into the deeper tracker/hours/internships page. Existing raw preceptor/site/agency Quick Stats cards and all workflow cards kept intact per the task's "keep cards intact" instruction. `docs/SITEMAP.md` updated for both pages + the new route. tsc clean, build green, eslint diff-clean vs pre-change baseline (`git stash` comparison — 3 new `any`-typed callbacks I introduced were given proper interfaces instead).
- `90326590` | **Feedback-triage bug fixes + exam-session editing.** Notion Task Handoff Queue items (FEEDBACK-BUGS, FEATURE exam-session edit). (1) `app/labs/acls-hub/page.tsx`: breadcrumb "Calendar" link pointed at the generic `/calendar` master view instead of the lab-filtered view — now `/calendar?preset=labs`, consistent with the hub's ACLS-lab-only scope. (2) `app/api/lab-management/students/[id]/route.ts`: the student-PATCH `allowedFields` whitelist never included `scrub_top_size`/`scrub_bottom_size` (both real columns) — the edit UI, hook, and PATCH body all already wired the fields, but the API silently dropped them, so edits looked like they saved but never persisted. Added both fields to the whitelist. (3) `components/scheduler/SchedulerCreate.tsx`: poll-creation Phase 1 (mode select) → Phase 2 (setup) transition didn't clear `creatorSelectedSlots` on Back or on mode re-selection, so slot indices from a prior mode/week-count carried over and no longer aligned with the new grid — now cleared on both mode buttons and the Back button. (4) `components/ChecklistAttendance.tsx`: checklist checkbox toggles could get wiped by a same-checklist re-fetch racing the optimistic update — added a `fetchedChecklistIdRef` guard (same pattern as the 2026-06-30 session-refresh-wipe fixes) so `fetchAttendance` only re-runs when the checklist ID actually changes, not on every re-render of `selectedChecklist`. (5) `components/TimerBanner.tsx`: the red "TIME TO ROTATE" alert reset on `timerState.status === 'stopped'` but not on rotation advance, so it could stay visibly stuck for the length of the next rotation after a manual advance — now also clears on `rotation_number` change. (6) `app/admin/exam-sessions/page.tsx`: the inline session-edit form (date/time/seats/notes) was missing the proctor field, even though the header row already had a working proctor-change dropdown and the PATCH API already accepted `primary_instructor_id` — added it to the edit form for consistency. **Investigated, not changed:** Ben's "timer only shows if grader marked ready" hypothesis does not hold in code — `TimerBanner` render is gated only on `station.lab_day.id` + `!is_nremt_testing`, never on ready state (see session notes for the observation this doesn't explain); the "timer starts at :49 not :59" and "rotation-duration-adjust only affects one rotation" reports could not be reproduced from the code (duration_seconds already persists across `next`/`prev` and `set_duration` already mirrors to `lab_days.rotation_duration` + all `lab_stations` rows) — flagged for Ben to reproduce with steps if still observed. Signature-pad clear bug (item 1 of the FEEDBACK-BUGS task) was already fixed in `cafb554` (2026-06-24), no action needed. `feedback_reports.status` not updated automatically — Notion task notes this is per-item, done via the Session Log. tsc 0 + clean build.
- `76fc7643` | **CLAUDE.md: prompt precedence over scheduled cadence.** New rule under Agent Autonomy Scope — the hourly Code routine / Cowork scan interval is a fallback safety net for when Ben isn't actively engaged, not a rate limit on responsiveness. A live prompt always takes precedence over waiting for the next scheduled run; after finishing a task with idle capacity, self-feed into the next in-scope board item rather than idling until the timer pops. Docs-only, no code change.
- `7037ec9d` | **fix(grading): GradingHeader Save button matches NREMT-strict/lenient rule (PR #9 follow-up).** The sticky-header Save button (`components/grading/GradingHeader.tsx`) always required `allRated` regardless of sheet type, contradicting the main-page Save buttons fixed in PR #9 (`4cff0981`, below) which only block on incomplete ratings for the strict NREMT E204 sheet. Derives `strictNremt` from the existing `nremtCode` prop, same pattern as `app/labs/grade/station/[id]/page.tsx`. tsc 0 + clean build; deploy verified green; migration for PR #9 confirmed already applied to production.
- `4cff0981` | **Scheduled-run fixes: grading rating-buttons wiped + not-satisfactory send blocked + ACLS post-save blank + timer discovery latency.** Root cause for the first three: the same NextAuth-session-refetch-on-window-focus class of bug already fixed once in this codebase (2026-06-30 entry), recurring in two more places. (1) `app/labs/grade/station/[id]/page.tsx`: added `prevStationIdRef` guard on the `[session, stationId]` fetchStation effect — previously every spurious session refresh re-fetched the station (new object reference), which cascaded into `buildInitialCriteriaRatings()` re-running and wiping in-progress S/NI/U ratings mid-grade (looked like the rating buttons had stopped responding). Also hoisted `strictNremt` to component scope and used it to gate the Send-button `disabled` conditions (`(strictNremt && !allRated)` instead of unconditional `!allRated`) — the 2026-06-17 "non-blocking generic sheet" change (`896253ca`) relaxed the internal validation for non-E204 sheets but left the button-level disable requiring every criterion rated regardless, so "not satisfactory + comments" still couldn't be sent. (2) `app/labs/adv-cert/grade/page.tsx`: the scenario-fetch effect already guarded `resetGrading()` against spurious session refreshes via `lastResetScenarioRef`, but still unconditionally set `loadingScenario(true)` and re-fetched on every refresh, which blanks the grading form (rendered only when `scenario && !loadingScenario`) for the duration of the pointless refetch — moved the ref check to guard the whole fetch, not just the reset. (3) `components/GlobalTimerBanner.tsx` + migration `20260701_lab_timer_state_realtime_rls.sql`: added a Supabase Realtime subscription on `lab_timer_state` (anon SELECT RLS policy + publication membership, mirroring the existing `lab_day_messages` pattern) so a started timer surfaces within seconds — additive alongside the existing 60s discovery poll rather than shortening it, since a shortened poll caused the 2026-05-26 production overload this exact interval was set to prevent. tsc 0 + clean build; lint diff-clean (no new issues vs. pre-change baseline). Live browser verification not possible in this sandboxed session (no Supabase/NextAuth credentials available) — recommend Ben confirm on the next grading session before treating as fully verified.

- `dba1a8b` | **Resolved-feedback digest email (build only, not yet live).** New `GET /api/cron/feedback-digest` (`app/api/cron/feedback-digest/route.ts`) sends a per-reporter weekly email of their own resolved `feedback_reports` items (description + resolution_notes), per Ben's decision on the Notion Task Handoff Queue feedback-loop-closure task (digest over per-item pings/in-app-view/changelog). Reuses the existing Resend send + `wrapInEmailTemplate` styling (same pattern as `app/api/cron/daily-digest`), respects the NREMT testing-day and student-email-blackout guards. New nullable `feedback_reports.digest_sent_at` column (migration `20260701_feedback_reports_digest_sent_at.sql`, additive/idempotent, ROLLBACK included) marks reports once emailed so re-runs don't double-send; **migration not yet applied to production** — this remote session has no `SUPABASE_DB_URL`, so it needs `node scripts/run-migration.js supabase/migrations/20260701_feedback_reports_digest_sent_at.sql` run from an environment with DB creds before the route can work end-to-end. Supports `?dryRun=1` for a no-send preview. **Deliberately NOT wired into `vercel.json` crons** — Ben flagged two open parameters in the task notes (weekly cadence vs. other cadence; per-reporter vs. team-wide digest) that still need his go/no-go before this runs live and starts emailing real reporters; see Notion task page for the flag. tsc 0 + clean build.

- `(pending)` | **Clinical consolidation: remove expiry Compliance Tracker + Compliance Docs page + dual-track booleans; per-cohort readiness rollup on overview.** Ben-approved removal (verified low-risk: `student_compliance_records` 0 rows, `compliance_document_types` 9 rows of type-defs only). **Migration** `20260701_remove_expiry_compliance_tracker.sql` (run with `--backup`) drops both tables; restore points `_backup_student_compliance_records_20260701050645` (0 rows) / `_backup_compliance_document_types_20260701050645` (9 rows). **Removed:** `app/clinical/compliance-tracker/page.tsx`, `app/clinical/compliance/page.tsx`, `app/api/compliance/route.ts`; the Compliance tab (interfaces, state, handlers, JSX) on `app/academics/students/[id]/page.tsx`; the two secondary nav cards on `/clinical`; two dead Breadcrumbs labels. **Redirects added** (`next.config.ts`): `/clinical/compliance-tracker` and `/clinical/compliance` → `/clinical/clinical-tracker`. **Retired** the dual-track booleans (`background_check_complete`, `drug_test_complete` on `student_compliance_docs`) — their only writer was the removed Compliance Docs page; columns left in DB per archive-don't-delete (portfolio/clinical-tasks routes still read them as a frozen historical snapshot, unaffected). **3 unmapped live dependencies found + fixed during the dependency sweep** (beyond the task's original scope, all safe subtractions since a working fallback path already existed): `app/api/cron/compliance-expiry/route.ts` (daily 8am cron — removed the dead-table "normalized schema" check, kept the working `student_compliance_docs` wide-table check); `app/api/student/my-progress/route.ts` (student-facing progress API — the compliance section was already defensively wrapped, now just returns `[]` with an explanatory comment); `app/api/lab-management/students/[id]/portfolio/route.ts` (already guarded/fell back correctly, removed the now-always-null normalized-records branch for hygiene). **Kept, unaffected:** `student_compliance_docs` + `student_mce_modules` (Clinical Tracker's tables), `/api/clinical/compliance` + `/api/clinical/mce-docs` (Clinical Tracker's APIs). **Per-student rollup:** confirmed the Clinical Tracker already writes per-student (`student_id`-keyed rows) — no new schema needed; `complio_complete`/`mce_complete` are Rae's existing manual master-checkboxes per tab. **Phase-segmented overview:** new `GET /api/clinical/tracker-readiness` (lead_instructor+) computes per-cohort "ready" counts (`complio_complete && mce_complete`) for every cohort using the tracker; wired into the existing S2 section of `/clinical/overview` (already phase-segmented by semester) as clickable `G14: 5/15 ready`-style chips deep-linking to `/clinical/clinical-tracker?cohortId=`. Naming ("Clinical Tracker" vs alternative) left deferred per task notes — not decided this session. tsc 0 + clean build.

## 2026-06-30

- `2bb27bcc` | **Scheduled-run fixes: grading session-refresh wipe, lab-groups duplicate-on-move, internship dedup + CLAUDE.md autonomy scope + doc updates.** (1) `app/labs/adv-cert/grade/page.tsx`: added `prevLabDayIdRef` guard — context selectors (group/station/team-lead) now reset only when `labDayId` actually changes, not on every spurious NextAuth session-identity refresh on window focus (Bug 1+2: "timer erases context" / "tab-switch loses scoring"). (2) `app/academics/cohorts/[id]/groups/page.tsx`: merged two sequential `setGroups(groups.map(...))` calls in `handleDropToGroup` into a single `setGroups(prev => ...)` functional update — both calls were closing over the same stale snapshot; second overwrote the first, leaving student visible in both groups until hard refresh (Data Bug 1). (3) `app/api/clinical/internships/route.ts`: added client-side dedup by `student_id` after Supabase query (when `cohortId` specified), keeping most-recent row per student — fixes G12 dual-tracking rows appearing twice (Data Bug 2). (4) `CLAUDE.md`: added "Agent Autonomy Scope" section (Bucket 1 / Bucket 2 model) + "Timestamp discipline" subsection. (5) `docs/DATABASE_SCHEMA.md`: added `lvfr_pharm_practice_sessions` table entry (was missing). (6) `docs/SITEMAP.md`: added explicit `/reports/instructor-workload` path reference.
- `bc8a6b13` | **Merge PR #7 to production — instructor-workload report + LVFR calendar sync.** Clean merge (no conflicts, no file overlap with the skills/clinical work already on main). `c4700ba` adds LVFR assignment → Google Calendar sync (`syncLvfrAssignment()` in `lib/google-calendar.ts`) + extended lab lookback, making the calendar the unifying layer for workload. `a5377c1` rewrites `/api/reports/instructor-workload`: drops the defunct `scheduling_polls`/`poll_submissions` block, queries live `lvfr_aemt_instructor_assignments` (LVFR at 3.5h/day), adds an LVFR-days column to `/reports/instructor-workload`, and adds the missing admin-hub nav card. No DB migration. tsc 0 + clean build; deploy green; `/admin`, `/reports/instructor-workload`, `/admin/instructor-workload` all 200.

## 2026-06-29

- `cd442865` | **Skills tracking Side 2 — completion export + blank reference sheets.** Side 2a: `GET /api/lvfr-aemt/skills/completion-export?cohort_id=` (instructor+) renders ONE PMI-branded completion summary per student (all 30 skills by category, ✓ + class-coverage date, instructor sign-off line) name-stamped into a per-student PDF, bundled as a `.zip` — reuses the AHA `aha/zip` puppeteer+JSZip pipeline. Side 2b: `GET /api/lvfr-aemt/skills/reference-export[?skill_id=]` (instructor+) renders the BLANK reference/failure-doc sheets, one printable page per skill (name, description, equipment, safety, intro/practice/eval days + blank ruled notes area + Pass/Fail + hand sign-off), as a single PDF. v1 reference content = DB fields only (per-step NREMT/PMI criteria can be layered in later). Both wired to "Export" buttons on the `/lvfr-aemt/skills` Class view. Render modules `lib/reports/lvfr-skills/{completionSheet,referenceSheet}.ts`. tsc 0 + clean build.
- `4dc7f190` | **Skills tracking Side 1 — class-level completion.** New `lvfr_aemt_skill_class_completion` table (cohort_id, skill_id, completed, completed_date/by, notes; UNIQUE(cohort_id,skill_id); FKs to cohorts + lvfr_aemt_skills; additive, RLS, ROLLBACK; migration `20260629_lvfr_skill_class_completion.sql`). New API `GET/POST /api/lvfr-aemt/skills/class-completion` (instructor+, non-agency). New "Class" view on `/lvfr-aemt/skills` (instructor default): roster-based checklist of the 30 skills grouped by category, cohort selector, per-skill "covered for the class" toggle (autofills completion for the whole roster — NOT per-student scored), introduced/practice/eval-day labels, class-completion % roll-up. Per spec: failures are a manual exception off the blank reference sheet, not tracked. Existing per-student Individual/Matrix views left intact. tsc 0 + clean build. (Side 2a ACLS-style export + Side 2b reference sheet pending Ben's template approval.)
- `7894120b` | **Merge PR #6 to main (production).** Clean merge (no conflicts; merge-base already carried PR #5's clinical card, so git removed the secondary and added the primary cleanly). Ran additive migration `20260629_lvfr_pharm_practice_sessions.sql`. tsc 0 + clean build. Deploy green; verified `/lvfr-aemt/day` 404 cleared (now 200), `/clinical` 200, `/lvfr-aemt/pharm/practice` 200 (public).
- `1a5c3b60` | **Merge PR #5 to main (production).** Clinical Tracker live; migration columns pre-applied; deploy green; clinical-tracker console 404s cleared.
- `5a25d458` | **Pharm: unauthenticated practice link.** New public page `/lvfr-aemt/pharm/practice` (no login): student enters name + identifier, picks difficulty, takes fill-in-the-blank quiz against all 8 medications, gets instant scored results. Results stored in new `lvfr_aemt_pharm_practice_sessions` table (migration `20260629_lvfr_pharm_practice_sessions.sql`). New APIs: `GET /api/lvfr-aemt/pharm/practice` (returns meds, no auth), `POST /api/lvfr-aemt/pharm/practice` (scores + saves, no auth), `GET /api/lvfr-aemt/pharm/practice/sessions` (instructor-only: list submissions). Updated authenticated pharm page (`/lvfr-aemt/pharm`) to show instructors a copyable practice link + table of all student practice submissions. Mobile-friendly layout (phone-first exception per task notes). tsc 0 + clean build. **FLAG for Ben**: results-delivery design — submissions now viewable in-app on the authenticated pharm page; ask Ben if he wants email notifications or a dedicated results view.
- `c800adc0` | **LVFR tuning + Clinical nav (Notion queue batch).** (1) Clinical hub: promoted Clinical Tracker to a 5th PrimaryCard (orange accent); removed it from SecondaryCard "More" list. (2) LVFR day page: 412 pre-start seed now shows a gentle "No program scheduled" note instead of error banner; added date-picker jump in header nav. (3) LVFR day: created `/lvfr-aemt/day/page.tsx` redirect-to-today to fix RSC 404 prefetch from Breadcrumbs link. (4) LVFR dashboard: reseed button moved from day view to admin-only section (admin+ gate, confirm-before-overwrite guard). (5) Skills matrix: CSV export button added to MatrixView filters bar. tsc 0 + clean build.
- `a2d5fd80` | **Feat: Clinical Tracker — Complio + mCE two-tab checklist.** New page `/clinical/clinical-tracker` with Complio and mCE tabs mirroring the Group 12/13 Google Sheets structure exactly. DB migration `20260629_complio_mce_tracker_3state_bg_dt.sql`: adds `background_check_status` + `drug_test_status` (3-state TEXT) to `student_compliance_docs`, migrating 17 existing TRUE rows to 'complete'; adds `bg_check_status` + `drug_test_status` + `mce_notes` + `nsp` to `student_mce_modules`; adds `chh_receipt_complete`, `chh_approval_complete`, `complio_notes` to `student_compliance_docs`. New API: `GET/POST /api/clinical/mce-docs` (wraps `student_mce_modules`). Updated `/api/clinical/compliance` GET to merge student name fields + include placeholder rows for students without a doc record yet. Aggregation overview with per-column progress bars (collapsible), printable per-student modal, per-tab notes cells (click-to-edit textarea). Nav entry added to clinical hub. DATABASE_SCHEMA.md, SITEMAP.md updated. tsc 0-equivalent + clean build.
- `d236bfad` | **Consolidate the 4 weekend draft PRs into main (overlap-aware).** Merged PR #2 (`/api/notifications/send-email` route — fixes the broken Send Email button; previously had no changelog entry), PR #3 (delete config-redirected dead scheduler pages + DEAD_CODE_REPORT), and PR #1 (canonical LVFR Tier-2 `activity` + 22-file lint pass + SITEMAP). **PR #4 was dropped, not merged** — its Tier-2 was a strict subset of PR #1's (PR #1 adds the `requirement` field, `defaultRequirement()`, the coverage-notes UI, and the activity badge that #4 lacked); merging both would have double-applied / conflicted. Verified before merge: the two Tier-2 migrations are functionally identical (took #1's); the live DB `item_type` CHECK already includes `activity` (pre-applied); the deleted `/scheduler` pages are genuinely dead (next.config 308 redirects shadow them). Only conflict was CHANGELOG (resolved, kept all entries). tsc 0 + clean build. Branch cleanup: merged branches #1–#3 deleted; PR #4 left for Ben to close as superseded.

## 2026-06-27

- `267747a` | **Feat: LVFR Tier-2 activities — H2 3-tier model complete.** Adds `'activity'` item_type to `lvfr_schedule_items` (migration `20260627_lvfr_activity_item_type.sql`). API: POST/PATCH now accept `requirement` explicitly; `defaultRequirement()` maps activity → optional, break → info, else → required. UI: "Activity (Tier-2)" added to item type dropdown; AddItemRow shows Coverage notes field + Tier-2 hint when type is activity; ItemRow shows purple "activity" badge (not double-badging with "optional"). 3-tier model fully live: Tier-1 (required/metered), Tier-2 (optional/trackable, not metered), Tier-3 (info/no checkbox). DATABASE_SCHEMA.md updated. tsc 0 + clean build. Per [WEEKEND] directive + LVFR H2 GO. (Consolidated from PR #1; PR #4's simpler Tier-2 stub was subsumed and dropped.)
- `11b47a2` | **Fix: clear all `react/no-unescaped-entities` lint errors (22 files) + SITEMAP update.** Batch-replaced bare `'`/`"` in JSX text content with HTML entities (`&apos;`, `&ldquo;`, `&rdquo;`) across 22 files: academics/cohorts, admin/bulk-operations, admin/poll, clinical/planning-calendar, clinical/site-visits, guest, instructor/time-clock, labs/schedule/new, poll, reports/clinical-hours, reports/internship-status, reports/lab-progress, reports/onboarding-status, reports/student-progress, reports/team-leads, resources/medications, resources, scheduling/availability, scheduling, scheduling/signups/pending, components/scheduling/GenerateLabShiftsModal, lvfr-aemt/progress. Zero functional impact — cosmetic HTML entity equivalents only; lint error count for this rule: 22 → 0. Also: `docs/SITEMAP.md` — added `/lvfr-aemt/progress` (Tier-1 coverage roll-up, instructor-only, added aefd339d), updated LVFR-AEMT page count 10 → 11, updated refresh date. Closes Documentation Update Rule gap from aefd339d. tsc 0. Weekend backlog item per [WEEKEND] directive.
- `1261284` | **Dead code cleanup: delete stale scheduler pages + update DEAD_CODE_REPORT.** Removed `app/scheduler/page.tsx` (288 lines) and `app/scheduler/create/page.tsx` (127 lines) — both unreachable via permanent 308 redirects (`/scheduler → /scheduling/polls`, `/scheduler/create → /scheduling/polls/create`). Updated `docs/DEAD_CODE_REPORT.md`: marked scheduler pages and Send Email button as resolved; confirmed all 53 unused imports and 13 orphaned components from the June-05 report are already gone; noted remaining open item (instructor-workload defunct report block needs Ben decision).
- `6a38e308` | **Fix: team_lead_log lab_day_id NOT-NULL (23502) — new production crash.** After the cohort_id fix (85d0b1e3), Vercel logs showed team_lead_log still 23502-ing, now on `lab_day_id` (null). Root cause: the station grading page (`app/labs/grade/station/[id]/page.tsx`) POSTed `student_id`/`lab_station_id`/`date` but **not `lab_day_id`** (and sent `scenario_type`, which the API ignores). Fixes: (1) client now sends `lab_day_id: station.lab_day.id` + `scenario_id: station.scenario.id`; (2) the team-leads POST validates `student_id`/`lab_day_id`/`date` up front → readable 400 instead of a DB 23502; (3) persists the `notes` the client was already sending (previously dropped). Bounded + reversible, no schema change. tsc 0 + clean build. (Triaged with the rest of the 3-day Vercel error report: ekg-scenarios `scenario_name` + completions PGRST200 are resolved by 85d0b1e3 — last seen pre-deploy; calendar 403 = Google reconnect (not code); `url.parse` DEP0169 = benign next-auth-internal deprecation warning.)

## 2026-06-26

- `aefd339d` | **LVFR runsheet H2 — Tier-1 coverage roll-up (v1) + 3-tier confirmation.** New read-only aggregation `app/api/lvfr-aemt/runsheet/progress/route.ts` + page `app/lvfr-aemt/progress/page.tsx` (linked from the LVFR hub, instructor-only): course-wide view of TRACKED Tier-1 items (chapters/quizzes/labs/exams/skills) — overall %, outstanding count, by-type breakdown, and per-day completion with the specific outstanding items (gaps), each day linking to its runsheet. Desktop-first. Tier-1 (tracked checkboxes) and Tier-3 (breaks/lunch → schedule markers, untracked) were already live from the H2 work; this adds the roll-up. **Flagged v1 for Ben's design review**; **Tier-2** (planned activities/group-tests = trackable + coverage text vs. unplanned = free text) is **proposed, not built** — its UX is the design-laden piece, held for Ben. (Task was board-assigned to Claude AI as an escalation; executed by Code per Ben's direct go.) tsc 0 + clean build.
- `d93f0dfb` | **Self-hosted Notion→Code webhook forwarder** (queue task, additive + reversible). New route `app/api/triggers/notion-webhook/route.ts`: receives Notion's Send-webhook automation, authenticates a shared secret (constant-time compare; `x-webhook-secret` header OR `?secret=` query param, since Notion can't reliably attach auth headers), and forwards a POST to the Code routine trigger with a bearer token. Secrets live ONLY in Vercel env (`NOTION_WEBHOOK_SECRET`, `CODE_ROUTINE_TRIGGER_URL`, `CODE_ROUTINE_TRIGGER_TOKEN`) — never logged. Returns 200 fast; 401 on bad secret; 503 if unconfigured. `GET` is a config health-check (no secret values). Replaces Zapier; lets a queue change fire the routine immediately instead of the hourly poll. No existing code/schema touched. tsc 0 + clean build.
- `da81cfca` | **Reversibility hardening — 3 self-side fixes from the foundation audit** (Notion queue task, code/doc only, reversible). (1) `scripts/run-migration.js` gains `--backup=t1,t2`: before applying a destructive migration it snapshots each listed table into an in-DB restore-point table `_backup_<table>_<timestamp>` (via `CREATE TABLE AS TABLE`) and prints the restore + cleanup SQL — "no destructive op without a restore point." Identifiers are validated against injection; dry-run reports what it would snapshot. (Used in-DB snapshots rather than `pg_dump` since pg_dump isn't guaranteed installed and in-DB snapshots are dependency-free + instantly restorable.) (2) `CLAUDE.md` Migration Reversibility (HARD REQUIREMENT): every migration carries a trailing `-- ROLLBACK:` reverse-SQL block (or a note if irreversible); applied the convention to `20260625_fix_station_completions_station_fk.sql` as the first instance. (3) `CLAUDE.md` archive-don't-delete rule: never autonomously hard-delete — soft-delete/archive or route through the `deletion_requests` superadmin queue; hard DELETE/DROP/destructive UPDATE = escalate-to-Ben + snapshot first. Flips audit items #2/#5 toward green and hardens #3. Remaining gate items (#1 Supabase PITR, #4 GitHub branch protection) are Ben dashboard actions.

## 2026-06-25

- `f9f8f5e9` | **Production error-log fixes (5 bugs from Vercel logs).** (1) **team_lead_log 23502** (`/api/lab-management/team-leads` POST): insert omitted the NOT-NULL `cohort_id` — now derives it from the body or the student's cohort (400 if the student has none). (2) **availability-reminders 22P02** (`/api/cron/availability-reminders` + the 2 sibling files `scheduling/send-availability-reminders` and `scheduling/availability-status`): the week date was written/queried into `user_notifications.reference_id`, which is **uuid**-typed. Moved the week key into `reference_type` (`availability_reminder:YYYY-MM-DD`) and dropped the date-in-uuid filter across all three files (fixed together for consistency, else dedup/status would diverge). (3) **station/completions PGRST200** (`/api/student/completions`): `station_completions.station_id` FK wrongly targeted `lab_stations` but the column references `station_pool` (writer validates against station_pool; routes embed it). Migration `20260625_fix_station_completions_station_fk.sql` re-points the FK to `station_pool` (ON DELETE SET NULL; table empty → 0 rows affected) — also repairs the same embed in `/api/stations/completions` and `/api/students/[id]/progress`. DATABASE_SCHEMA.md already documented the intended `→ station_pool` FK (DB was the outlier). FK-ambiguity re-checked: the `station_pool` embed is single-path, sibling route already uses explicit `!fk` hints. (4) **ekg-scenarios 42703** (`/api/student/ekg-scenarios`): removed the non-existent `scenario_participation.scenario_name` from the select (the embedded `scenario.title` supplies the name). (5) **student_notes 23502** (`students/[id]/notes` POST): populate the legacy NOT-NULL `created_by` (mirrors `author_email`). Confirmed non-code (per logs): calendar 403 = Google reconnect (not route logic); next-auth "State cookie missing" = likely tablet-login OAuth — flag if you want it chased. tsc 0 + clean build; migration dry-run then applied.

- `14591502` | **LVFR AEMT runsheet polish (4 fixes + Drive link) for the July 14 start.** Instructor-only daily coordination hub. **H1 day order:** already correct (API orders morning-first, page renders morning→lunch→afternoon) — verified, no change. **H2 checkbox fatigue:** the seed never set `requirement`, so every item (incl. breaks, which default to `required`) showed a checkbox. Seed + manual-add now set `requirement` — major academic tasks (chapter/quiz/skills/lab/exam) → `required` (checkbox + counted); breaks/lunch → `info` (no checkbox). **PROPOSED CUT (flag for Ben):** generic `other` blocks also → `info` — one-line revert in `requirementForType` if any should stay tracked. Re-seed backfills already-seeded days (downgrades seeded break/other to info; leaves manual ad-hoc adds and academic items). **H3 brief + debrief:** display existed but couldn't be edited — added editable "Today's brief" (top, on the morning row) and "Debrief" (bottom, on the afternoon row); PATCH now accepts `brief`/`debrief` (field-scoped so it never clobbers notes). **H4 schedule:** seed now sets each item's `time_label` from block start/end so the day reads as a timeline (with the existing session time-headers + lunch banner). **Drive link:** added "Lesson plans & slides" (→ LVFR AEMT Group 2 "Daily folder", per-day Day 1–30 subfolders) + "Lesson plan doc" links in the action row. Per-day deep-links deferred — Drive day folders are keyed by program day number, not calendar date (no clean date→Day-N map); folder link is the agreed fallback. Desktop-first. tsc 0 + clean build.

- `3222e80c` | **Scenario follow-up #1 (EXPANSION): `instructor_cues` is now a first-class phase field + displayed.** Previously folded into `expected_actions` as a bridge. Now: (1) `instructor_cues` is a distinct `string[]` on the phase model — kept separate everywhere (removed the fold in `lib/scenario-validate.ts normalizeScenarioPhases` and in the edit page's `normalizeLoadedPhase`); (2) carried through `bulk-import buildPhases` (+ that path now also maps builder phase aliases `name←title`, `presentation_notes←presentation_text`, vitals `blood_glucose←glucose`/`gcs_total←gcs` so bulk import stops dropping them too); (3) editor gets an "Instructor Cues" textarea (one per line); (4) **displayed** in the scenario reference (`ScenarioFullDisplay`), the grading view (`ScenarioGrading`), the edit-page phase summary, and the printable scenario sheet; (5) blank cue lines stripped on save. `presentation_notes` confirmed stored + displayed (it already was). Builder doc updated: `instructor_cues` resolved from "decision pending" to a documented first-class field. tsc 0 + clean build.
- `7c20f274` | **Scenario follow-ups #2 (bulk-import parity) + #3 (blank-field robustness).** **#2:** the bulk importer silently DROPPED fields the DB has columns for and the scenario-screen path persists. Added them to `ImportedScenario`/`ParsedScenario` + the parsed builder + commit `rowData`: `patient_presentation`, `history`, top-level `vitals`, `gcs`, `pupils`, `general_impression` (now prefers top-level over phase[0]), `assessment_x`–`assessment_e`, `secondary_survey`, `expected_interventions`, `equipment_needed`, `medications_to_administer`, `opqrst`, `ekg_findings` (now prefers a top-level object), `preferred_manikin`, `content_review_status`. Also `allergies` array → joined string (text column) — covers follow-up #3-allergies on the import path. (`evaluation_criteria` intentionally not persisted — no DB column.) **#3 robustness (blank field must never crash):** audited all scenario render/import/edit paths (3 agents). Render + edit/create already null-safe; import-path guards added — `bulk-import buildPhases` `(imported.phases||[]).map` and the `imported.phases.length` check → `Array.isArray`; `lib/reports/aha/megacodeForm.ts` `seg?.criteria[i]` → `Array.isArray(seg?.criteria)` (AHA megacode render); `admin/scenarios/audit/route.ts` `Object.keys(v)`/`Object.keys(phases[0])` → null/non-object guards. tsc 0 + clean build. (Bulk-import full-replace-on-update semantics preserved.) Follow-up #1 (real `instructor_cues` phase column + display) still queued.
- `6e07b22f` | **Fix: scenario-screen edit crash on import-shaped phase vitals (the actual round-trip crash site).** Narrowed via the offending JSON: the SAME file imports fine via the bulk importer but crashed the scenario edit screen. Root cause: the bulk importer's `buildPhases` always builds a complete phase-vitals object (incl. `other_findings: []`), but the edit page (`app/labs/scenarios/[id]/page.tsx`) loaded `s.phases` **raw**, so a builder/import phase whose `vitals` lacked `other_findings` hit `vitals.other_findings.length` (unguarded, line ~713) → "Cannot read properties of undefined (reading 'length')". Fix: `fetchScenario` now runs each loaded phase through new `normalizeLoadedPhase`/`normalizeLoadedVitals` helpers (mirror `buildPhases`: guarantee `other_findings` is an array; also map builder aliases `name←title`, `presentation_notes←presentation_text`, `expected_actions←instructor_cues`, vitals `blood_glucose←glucose`, `gcs_total←gcs` so nothing is dropped/blank), and `VitalsEditor` defaults `other_findings` to `[]` defensively. Aligns the edit path's tolerance with the (working) bulk importer. tsc 0 + clean build. (Bulk importer left untouched per request. Also: confirmed the bulk-import commit already upserts on top-level `id`, and the exporter already emits `id` — the "duplicate" was a false alarm: the Anterior/Lateral STEMI Flash-Pulm-Edema pair are distinct originals from 2026-01-13, not a round-trip dup. Builder doc updated separately to add top-level `id` + phase field-name alignment.)
- `a95981c4` | **Fix: scenario JSON round-trip upload crash + import shape validation.** A scenario re-uploaded after an external/LLM edit crashed the client with "Cannot read properties of undefined (reading 'length')". Root cause: the upload path (scenario edit page → PATCH `/api/lab-management/scenarios/[id]`) stored `body.phases` **as-is with no shape validation** — an enrichment that reshaped phases (e.g. renamed `name→title`, `presentation_notes→presentation_text`, `expected_actions→instructor_cues`) persisted, and render code read the now-missing fields. Fixes: (1) new `lib/scenario-validate.ts` — `validateScenarioShape` (readable errors: "field X expected an array, got Y"; checks array fields, object fields, phases) + `normalizeScenarioPhases` (maps well-known phase-field aliases back to the rendered schema, non-destructive, alias-only so normal scenarios aren't rewritten). (2) Wired validate→reject(400)→normalize into the PATCH save and POST create routes; guarded the `body.phases.length` access with `Array.isArray`. (3) `scripts/fix-scenario-phase-aliases.js` — idempotent repair for any already-saved aliased rows (found 0 live; the one affected record was corrected before this ran). The render components (`ScenarioFullDisplay`, `ScenarioGrading`) were already null-safe (`Array.isArray`/`||`/optional chaining); the gap was the import path, now closed. Protects Ben's regular export→enrich→re-upload workflow. tsc 0 + clean build.

## 2026-06-24

- `4290baaa` | **Background batch fixes (5 items).**
  - **#5 deletion-request queue (root cause + fix):** the DELETE endpoints for students, scenarios, cohorts, lab days, and internships rejected non-superadmins with a 403 ("requires approval via deletion requests") but **never inserted a row into `deletion_requests`**, so the admin queue was always empty. Added `lib/deletion-requests.ts` (`createDeletionRequestIfAbsent`, deduped + best-effort) and wired it into all 5 endpoints' non-superadmin branch (creates the pending request, then returns the same 403 so UI messaging is unchanged). Added `internship → student_internships` to the approval handler's tableMap so approved internship deletions actually delete.
  - **#2 false clinical-hours "behind" flags:** `app/clinical/hours/page.tsx` `getStudentPace()` flagged Sem 1/2 cohorts as "behind" (0 hours vs expected). Added a clinical-phase gate (`current_semester >= 3`) mirroring the existing cron gate; pre-clinical cohorts now show "unknown" pace, not "behind".
  - **#3 group display order (1,4,2,3):** root cause was **corrupted `display_order` data** (G15 "Group 4" at order 2 with a tie; G4 also drifted), not code. Normalized `display_order` to match the group-name number via `scripts/normalize-group-display-order.js` (idempotent; fixed 5 rows). Now sorts 1,2,3,4 for every consumer regardless of sort key.
  - **#1 equipment/costs/calendars errors:** data layer verified sound (direct inserts to `lab_day_equipment` + `lab_day_costs` succeed; calendars route already degrades gracefully). Hardened the equipment POST against a stale/invalid `station_id` FK (was a possible raw 500 → now ignored, item attaches at lab-day level). Remaining 500s/502 not reproducible server-side (likely transient/stale-deploy) — flagged for live error-body/logs.
  - **#4 S3/S4 clinical tag:** semester→phase label code is correct everywhere (S3=Clinical, S4=Internship); no inverted/off-by-one mapping exists. Issue is data-driven (`cohorts.current_semester`) — flagged for which cohort/page looks wrong (current data: G12/G13=S4 Internship, G14=S2, G15=S1, none at S3).

## 2026-06-23

- `50f74a61` | **AHA export: stamp the cohort course date on the 3 skills sheets (consistency with megacode)**. The skills sheets (Airway, Adult BLS, Infant CPR) had blank date fields; now their **Date of Test + instructor sign-off Date auto-fill with the cohort's course date** = the earliest ACLS lab-day (Day 1, when skills are verified) via new `fetchCourseDate()`. Threaded `courseDate` through `renderSkillsDocument` / `renderSkillsStudentSheet` / `composeStudentPacketHTML`, and computed it in both the main render endpoint and the ZIP endpoint (per-student packet + by-section). Validated vs G14 (course date 2026-06-18 stamped on all 3 skills sheets). **Confirms the full 4-form set is consistent:** skills sheets already got the same AHA red/cream colors (`#ca171e`/`#efe8d5`) and the same instructor sign-off autofill (name/AHA#/signature) as megacode last turn; now they also share the date stamp. (Megacode keeps its per-attempt station date for Date of Test/sign date.) tsc 0 + clean build.

- `8615fde2` | **AHA export: match the official AHA form colors (red headers + cream body)**. Extracted the real fill colors from the official 2025 PDFs via pdfplumber (rect `non_stroking_color`): section-header bars = **AHA red `#ca171e`** (was a wrong teal), body shading = **cream `#efe8d5`**. Applied to both `megacodeForm.ts` and `skillsForms.ts` (header rows red + white text, criteria step cells cream), with `print-color-adjust: exact`. **Removed the @media-print gray override** so the color now carries into the SAVED PDF (browser Save-as-PDF and the server ZIP) — a physical printer can still grayscale at print time. Fixed the skills section-caption color for contrast on red. Close-proximity match (not exact replication; AHA logo/trademark intentionally not reproduced). tsc 0 + clean build.

- `67f7c20e` | **AHA export: ZIP of pre-separated per-student PDFs (kills the ~2hr Adobe-extraction task)**. New `app/api/reports/aha/zip` (staff, `maxDuration 180`) renders REAL PDFs server-side via headless Chrome and bundles them with JSZip — reusing the proven `pdf-packet` pattern (`@sparticuz/chromium` + `puppeteer-core` + `jszip`, all already installed). **`grouping=student`** (default, the high-value one): one combined PDF per student — megacode + airway + adult BLS + infant CPR — named `LastName_FirstName_ACLS_Results.pdf`, drag-ready into student folders; excused/no-attempt students omitted. **`grouping=section`**: one PDF per form type (all students) for AHA records. New `lib/reports/aha/packet.ts` composes a single per-student HTML doc (merges both form modules' CSS, one `<head>`); exported per-student renderers + CSS from `megacodeForm.ts`/`skillsForms.ts` to support it. Picker (`/reports/aha`) gains a "Download all 4 forms as a ZIP" card (grouping selector + Download), alongside the existing single-form Open/Print. Validated packet composition vs G14 (4 forms/student, single head, 22 PDFs + 1 excused omitted); the puppeteer→PDF→zip machinery mirrors the working NREMT packet route. tsc 0 + clean build. (Collapses ~2hr extraction → ~15min unzip-and-drag. Deferred niceties: per-student subset "some" selection; single-combined-all-students PDF.)

- `cafb5540` | **AHA export: visual-QA fidelity fixes + signature/profile UX bugs**. **Megacode form** (`lib/reports/aha/megacode.ts`, `megacodeForm.ts`): (#1) auto-populate **Date of Test + instructor sign date** from the attempt's lab-day (station) date; (#2 — accuracy) "Chest compression fraction" and "Ventilation rate" now render as **fill-in values defaulted to passing** (`>80%`, `10/min`), not checkboxes, matching the official write-in blanks; (#3) **colored (teal) section-header bars on the saved/screen copy**, with a print override back to B&W; (#4) deliberately **no AHA logo/trademark** (IP) — colors/layout only; (#5) added the **"Learning Station Competency" footer** (Bradycardia / Tachycardia / Cardiac Arrest-PCAC / Megacode Practice). **Signature/profile bugs:** (#6) added a discoverable **AHA Instructor Credentials** link in Settings → Profile (was only reachable from the export section + UserMenu); (#7) fixed `SignaturePad` **clear** to reset the transform and clear the full backing store (was leaving ~¼ of the drawing); (#8 — real bug) **number and signature now save independently** on `/settings/aha-credentials` (separate "Save number" / "Save signature" actions, each PATCHing only its own field) so updating one no longer wipes the other. Validated megacode render vs G14 (dates, fill-ins, footer, color all present). tsc 0 + clean build.

- `30f4e19b` | **AHA megacode: manual tachy back-fill for Esther Mills (the 1 remaining needs-marking)**. Per Ben's review call, marked "Recognizes symptoms due to tachycardia" = met for Esther Mills' CASE_67 attempt (tachy section was explicitly failed but overall passed — the one record excluded from the automatic passer back-fill). Targeted/idempotent (`scripts/backfill-mills-tachy.js`, dry-run→applied). Result: **needs-marking now 0 across the cohort** — all 22 students with attempts render complete megacode forms (+1 excused = Cian). Also updated the OneDrive `acls_scenario_seed.json` so its `tachy_mgmt_unstable` carries all 5 criteria (verified seed == active DB rubric) → a future re-import no longer reverts the criterion/back-fill. Data-only; no code change.

- `550e21d9` | **AHA megacode: back-fill the new tachy criterion as MET for G14 tachy-section passers (benefit-of-the-doubt, system-side gap)**. Switched the retroactive handling from needs-marking → **back-fill** for passers, per Ben: "Recognizes symptoms due to tachycardia" was added after grading (an IT/system timing gap, not a performance gap), and passing the tachy section already entails recognizing the symptoms, so back-filling marks explicit what passing required. `scripts/backfill-tachy-passers.js` (dry-run→applied, idempotent) inserted `met=true` for the criterion on **18 tachy-section passers** (4 explicit section-pass + 14 null-section-result-with-overall-pass = benefit of the doubt, since the unset section result is itself a system-side gap). **Excluded the 2 explicit section-fails** (recognition not presumed where the instructor failed the section) → they remain needs-marking. Verified: passer forms now render the tachy section fully checked (5/5); needs-marking dropped 14→1 (the one section-fail), summary unchanged (11/11/1). Data-only (renderer already reads recorded/met); no code change. Standing rule recorded: system/IT-side errors → student gets the benefit of the doubt. (Same OneDrive-seed flag still applies for the criterion itself.)

- `20287459` | **AHA megacode: add the tachycardia "Recognizes symptoms due to tachycardia" criterion (match official) + robust criterion mapping**. Per the standing rules (if it's on the AHA rubric it should be graded; test/official criteria are authoritative): added the missing 5th item to the `tachy_mgmt_unstable` segment at display_order 4 (cardioversion → 5) via `scripts/add-tachy-criterion.js` (dry-run→applied, idempotent), so the rubric now matches the official Tachycardia Management section. Refactored `lib/reports/aha/megacode.ts` to build each attempt's criteria from the segment's CURRENT criteria definitions (ordered by display_order), each annotated with a `recorded` flag — robust to criteria added after grading, and no longer order-fragile. **G14 retroactive handling (chosen: needs-marking, NOT back-fill):** the 14 students with a tachy section were graded before this item existed → it renders as "▢ needs marking" (+ "† added after this attempt was graded" footnote), never a false miss or a fabricated pass; cardioversion still correctly checks. Removed the old "not auto-scored" footnote (the item is now a real graded criterion). Validated vs G14 (14 needs-marking, summary unchanged 11/11/1). tsc 0 + clean build. **⚠ FLAG: the OneDrive `acls_scenario_seed.json` still has 4 tachy criteria — a re-import would DEACTIVATE this 5th item (importer deactivates criteria beyond the seed). The seed's `tachy_mgmt_unstable` must get this 5th criterion before any future import.**

- `81a13b40` | **AHA megacode: resolve the 9 "flagged" students via section-level mapping (per AHA practice-as-testing rule)**. Verified in the live DB that rhythm-section criteria are SHARED segments (one each for bradycardia/vf/pvt/pea/asystole/pcac/team_leader/cpr_quality → criteria identical across practice + testing + official form). Only `tachycardia` has 2 segments (`tachy_mgmt_unstable` ×7, `tachy_mgmt_svt` ×1) — but the divergent svt one is **only on the inactive `MEGACODE_TEST_9`**, so **no active scenario / no real student is affected**. Therefore a student whose best attempt is on a non-named chain still populates the official form VALIDLY section-by-section (AHA permits practice scenarios as testing cases). Dropped the "not an official variant — verify" warning + the per-student flag; these now render as valid "practice case X used as testing scenario (scored by rhythm section)", labeled by rhythm chain. Summary reclassified `cleanMapped/flagged` → `namedVariant/sectionMapped` (G14: 11 named + 11 section-mapped + 1 excused, **0 warnings**). Validated vs G14. tsc 0 + clean build. (Confirms the modular by-section grading architecture. Remaining open item: tachycardia's official 5th item vs rubric's 4 — awaiting Ben.)

- `6c3729b9` | **AHA export — Infant CPR skills checklist (4-form set COMPLETE)**. Added `INFANT_CPR_FORM` to `lib/reports/aha/skillsForms.ts` from the clean pdfplumber-extracted structure (the prior pdftotext garbled the nested cells): Assessment + Cycles 1–4, **11 scorable checkboxes** with all compression/breath sub-items, plus the two "this rescuer is not evaluated" lines and instructor-prompt/transition text rendered as note rows (new `Section.notes` + `.noterow` style). Auto-completes as PASS like Airway/Adult BLS. Registered (`template=infant_cpr`) + added to the picker. Validated vs G14 via tsx (23 sheets, 11 steps + PASS each, 46 not-evaluated notes). **All 4 AHA forms now build** (Megacode + Airway + Adult BLS + Infant CPR). tsc 0 + clean build. Pending: tachycardia rubric item + 9 flagged students (awaiting Ben), and a visual QA pass vs the official layouts.

- `e58e36c3` | **AHA export — Checkpoint 5: picker UI + per-form instructor selection (export now usable)**. New page `app/reports/aha` (desktop-first): pick cohort + form (Megacode / Airway / Adult BLS) + signing instructor (from those with AHA credentials on file), then Open & Print → opens the autofilled print-to-PDF document in a new tab. Options endpoint `app/api/reports/aha/options` (cohorts + AHA-credentialed instructors). Per-form instructor selection = pick the signer per export (re-select & re-open for a different signer — keeps a recerting instructor off their own form). Surfaces guidance (megacode flagged/excused handling; skills auto-PASS; Infant CPR pending). Nav entry added to the Reports hub (`ClipboardSignature` card). SITEMAP updated. tsc 0 + clean build. **AHA export is now end-to-end usable for a cohort** (Megacode + Airway + Adult BLS); remaining: Infant CPR form (flagged), and a visual QA pass of the rendered PDFs against the official layouts.

- `1bcf9aa0` | **AHA export — Checkpoint 4 (partial): Airway + Adult High-Quality BLS skills checklists (auto-complete PASS)**. `lib/reports/aha/skillsForms.ts` recreates the official 2025 Airway Management and Adult High-Quality BLS skills checklists as HTML, **auto-completed as PASS** (every step checked) — documenting competency verified live during the course (these skills aren't scored in-app; a caption states this is documenting verified competency, not a fabricated score). Item text transcribed from the official PDFs. Shared `lib/reports/roster.ts` (`fetchScopeStudents`) drives per-student sheets; render endpoint `app/api/reports/aha` now serves `template=airway|adult_bls` (+ megacode). Validated vs G14 via tsx (23 sheets each, all steps checked, PASS, caption present). tsc 0 + clean build. **Infant CPR deferred + FLAGGED**: its per-cycle compression/breath sub-criteria did not survive PDF text extraction (nested/multi-column cells) — needs a cleaner source or visual confirm before faithful recreation.

- `d3adf7f5` | **AHA export — Checkpoint 3: Megacode Testing Checklist (HTML recreation + autofill + render endpoint)**. `lib/reports/aha/megacodeForm.ts` recreates the official 2025 AHA Megacode Testing Checklist (all 6 rhythm-chain variants; item text transcribed verbatim via pdftotext). Official items carry an explicit `dataIndex` into the recorded segment criteria — sections align 1:1 EXCEPT Tachycardia (official "Recognizes symptoms due to tachycardia" has no rubric counterpart → rendered unchecked with a "† not separately scored" footnote, not mis-aligned). Renders from the Checkpoint-1 best-attempt data: filled checkboxes, PASS/NR from overall_result, per-student flags, excused/no-attempt handling (no broken blank form), and a sign-off line (instructor name/AHA#/signature, blank until selected). Render endpoint `app/api/reports/aha` (staff) returns a self-contained print-to-PDF HTML document (NREMT pattern), scope = cohort or student, optional `instructorId` + `print=1`. **Validated end-to-end against G14 via tsx**: 23 forms (11 auto-mapped, 11 flagged-for-manual-variant, 1 excused=Cian), variant titles correct, checkmarks/PASS/footnotes/excused all render. tsc 0 + clean build. (Reachable by URL now; picker UI + per-form instructor selection land in CP5.)

- `e4918a02` | **AHA export — Checkpoint 2: instructor AHA credentials (profile + signature)**. Migration `20260623_lab_users_aha_profile.sql` (dry-run → applied): additive `lab_users.aha_instructor_number / signature_data / signature_kind` (nullable; existing RLS covers them). New self-service API `app/api/profile/aha` (GET/PATCH own record; validates signature is an image data URL ≤600KB, kind ∈ drawn|uploaded|auto). New page `app/settings/aha-credentials` to set AHA # + signature — draw (`components/SignaturePad.tsx`, canvas/pointer), upload an image, or auto (name in a script font) fallback. Nav entry added to UserMenu ("AHA Credentials"). These autofill the AHA export forms' "Instructor Initials / Instructor Number / Date" sign-off line. Schema doc + SITEMAP updated. tsc 0 + clean build green. (Form-scope confirmed = the 4-form set; official 2025 AHA PDFs received — all 4 carry the sign-off line; Learning Station checklists out of scope.)

- `260d20e6` | **AHA export — Checkpoint 1: engine + megacode data layer** (`lib/reports/engine.ts`, `lib/reports/aha/megacode.ts`). Pluggable per-student/per-cohort report engine (registry + scope; renders via the NREMT HTML→print-to-PDF pattern; AHA = first branch, course-agnostic). Megacode data layer: read-only fetch of scored attempts (practice valid for grading per AHA; testing used when present), best-attempt selection (PASS>FAIL then most criteria met), rhythm-chain→AHA-variant mapping (6 official variants), per-attempt segment/criteria assembly, and flags (unmapped chain → manual variant; no-pass; no-attempt). Logic validated against G14 (13/23 clean-mapped, 9 unmapped-pass, 1 no-attempt). No schema change, no DB writes. Render endpoint + form templates pending Ben's official 2025 AHA PDFs. tsc 0 + clean build green.

- `f61a7f11` | **AHA Results Export — discovery + spec checkpoint → `docs/AHA_EXPORT_SPEC.md`** (no build yet). Confirmed (read-only) the grading data maps for autofill: `adv_cert_test_attempts` (overall_result 31 pass/1 fail, team_lead_id always set), `adv_cert_segment_results`/`adv_cert_criterion_results` (met, via segment_result_id), `is_critical` exists but no megacode criteria are flagged (tiebreak → total met-criteria), rhythm chain derives from the assembled segment `algorithm_type` chain (`sequence_label` is not a DB column). Engine = first instance of a general per-student/per-cohort export/report engine, reusing the `nremtExport.ts` pattern (server-rendered styled HTML → browser print-to-PDF; pluggable template registry; AHA = detailed-form branch, PALS/clinical/team-lead plug in later). Spec covers the 4 forms, best-attempt selection, skills auto-complete, instructor-profile additions (aha_instructor_number/signature — additive, dry-run before apply), per-form instructor selection, per-student + whole-cohort output. Flags: some practice chains don't map to an AHA variant; need Ben's official 2025 AHA form templates. Tooling present: html2pdf.js + puppeteer-core.

- `3c3525fd` | **Desktop-first layout audit (read-only) → `docs/DESKTOP_FIRST_AUDIT.md`**. Swept ~280 `app/**/page.tsx` against the new UI Layout Rule. 39 flagged (38 HIGH primary management/data pages, 1 MED), ~240 already compliant. All 39 are the mobile-first grid idiom (`grid-cols-1` scaling up via `sm:`/`md:`/`lg:`) — most still render multi-column on desktop, so this is a consistency pass (invert to wide-base + `max-*:`), not broken-on-desktop pages; no narrow-container/single-column-only data pages found. Doc is a checkbox punch-list grouped by area (labs first), each with evidence + the one-line fix. No page changes made.

- `eb5e5a6a` | **Exam sign-up is now fully editable by admins (Rae)**. Closed the "can't edit after built" gap on `app/admin/exam-sessions/page.tsx`. **Session details:** per-session **Edit** toggle → inline form for date / start / end / total seats / Pima computers / notes (location lives in notes), wired to the existing `PATCH /sessions/[id]` (which already accepted these — it was a UI gap). Reducing capacity below current sign-ups shows a **confirm-warning** (no student removed — seats preserved — it just over-books until you move/remove someone). **Students:** per-signup **Move to…** (any other session) + **Remove** (X) buttons, backed by a NEW admin route `app/api/exam-scheduling/signups/admin/[id]/route.ts` (`PATCH` move + `DELETE` remove, `requireAuth('admin')`) — the existing `/signups/[id]` route is student-owner-only, so admins couldn't touch others' signups before. Move/remove release+take seats with capacity re-check, notify directors, update the exam-date write-back, and audit-log. **Date sync:** editing a session's date now re-writes `written_exam_scheduled` for all confirmed signups on it, keeping internship records accurate (matches the student-reschedule path). No schema change (location stays in notes). Scope: admin-built exam sign-up only — NOT student self-scheduling. tsc 0 + clean build green.

- `dcd9cc26` | **CLAUDE.md: add "UI Layout Rule (HARD REQUIREMENT)" — desktop-first / wide, not mobile-first**. Codified the standing build convention (from `PMI_Project_Instructions.md`): PMI is used primarily on desktop/laptop/iPad-landscape (esp. labs), phone is occasional fallback. Pages must start from the wide multi-column layout and adapt DOWN — Tailwind base classes = wide, `max-sm:`/`max-md:` to stack — not mobile-first `grid-cols-1` scaling up. Management/admin interfaces (scheduling, grading, ACLS hub, results, schedule-building, lab management) are the primary use and must be wide. When touching any page, flag any still stuck vertical/mobile-first. Added a Conventions pointer too. Instructions/docs only — no code.

## 2026-06-19

- `ff72dd77` | **Fix 2 live Grade (ACLS) workflow bugs (`app/labs/adv-cert/grade/page.tsx`)**. **Bug 1 (data loss):** the scenario-load effect called `resetGrading()` and depended on `session`; next-auth refreshes the session object on window-focus (e.g. when the grader flips to the timer tab and back), which re-fired the effect and **wiped the in-progress score sheet**. Fixed with a `lastResetScenarioRef` guard so `resetGrading()` runs ONLY on a genuine scenario change, never on a spurious refetch/re-render. **Bug 2 (re-nav after save):** `handleSave` success cleared `stationId/scenarioId/scenario/groupId`, blanking the form and forcing re-selection every save; now it clears only the score sheet + per-student selection (team lead / members) and **keeps day/group/station/scenario** so the grader is ready for the next student at the same station. Neither fix touches the save payload or grading logic. tsc 0 + clean build green.

- `3222c400` | **Day-2 megacode clean structure — 2 practice sets + paired testing**. `scripts/setup-day2-megacode-structure.js` (transactional + `--dry-run`). **Practice → 2 timed sets:** Set 1 = sec2 (relabeled "Megacode Practice — Set 1", retimed 08:30–10:00, stations 67/68/69/70 unchanged → its 7 instructor assignments preserved); Set 2 = NEW section (sec4, 10:15–11:45, stations **71/72/73/74**, mirrors Set 1's rotation params; instructors assigned live). **Testing → paired** (re-pointed sec3 in place so the 7 assignments survive): #1/#2 → **MEGACODE_TEST_1**, #3/#4 → **MEGACODE_TEST_3**; retest pool TEST_2/TEST_4 stays off-station. **Monolith** sec1 (the 16-station leftover, 0 assignments, already hub-hidden) relabeled **`[ARCHIVED]`** so the floor can't wander in. Day-2 schedule blocks rebuilt for the two practice sessions (Set 1 → Break → Set 2 → Break/Setup → Lunch → Testing → Exam → End 15:15). **Verified:** practice 67–74 + testing 1/3 show content; testing paired correctly; all 14 instructor assignments intact; 0 duplicate case_codes; no 48/49 collision (48/49 are the 2025 cardiac-arrest learning cases, 67–74 are practice). Builds on the consolidated push `d6a8f079` (renumber already applied). DB only — no app-code change.

## 2026-06-18

- `d6a8f079` | **ACLS 2025 consolidated scenario push (data only — schedule blocks untouched)**. Ran the consolidated push (`CODE_PUSH_consolidated.md`) as DATA, deliberately **skipping the schedule-seed block rebuild** (it would clobber the live restructure `2f54a3da` + its cardiac station list 45/47/67/68 contradicts the doc's 45/48/47/49; our stations link by `scenario_id` so there is no dangling-ref risk to avoid). Sequence: (1) `renumber-megacode-practice-67-74.js` — renamed megacode-practice scenario rows `CASE_48–55 → 67–74` (new `scenarios.legacy_code_2020` column records the old code; rows kept so the Megacode Practice station links followed to 67–70), freeing 48/49. (2) `import-acls-seed.js acls_scenario_seed.json` — 4 new (cardiac OOH `CASE_45/47/48/49`) + 16 updated (67–74 now carry real 2025 cards; megacode testing + brady/tachy refreshed), 20/20 narrative. (3) `cleanup-acls-2025-push.js` — deduped 4 stray `sequence_order=0` team_leader rows the import re-introduced on 67–70; **re-pointed the Day-1 Cardiac Arrest station (sec2) → CASE_45/48/47/49** (2 VF/pVT + 2 Asystole/PEA); deactivated orphaned 2020 cardiac `CASE_34/36/39/40` (reversible). **Verified (`verify-acls-2025.js`, `diag-megacode-rubrics.js`):** all 4 scenario sections show content; **0 duplicate case_codes**; megacode scoring-vs-rhythm assemblies intact through the renumber (testing 1–4 + practice 67–74 each its own rhythm-matched rubric, no generic shared rubric); 0 dangling refs on the real sections (remaining dangling = pre-existing empty stations on unrelated future labs + the hidden monolith sec-1 fallback pointing at the retired 2020 cases — not user-facing). **#7 instructor controls:** per-card `instructor_notes` DO render at the station (`ScenarioFullDisplay`); **live vitals adjustment is NOT a webapp capability** (read-only presenting vitals + phases; no interactive editor) — flagged as a feature gap; the seed's singular `instructor_note`/`vitals_instructor_adjustable` are not mapped by the importer (only the `instructor_notes` array is). Scripts only — no app-code change; Day-1/Day-2 schedule blocks (`d619fca7`) untouched.

- `2f54a3da` | **Day-1/Day-2 ACLS schedule restructure (2025 "as actually run", live spec)**. Reshaped G14 Day-1 + Day-2 blocks (`scripts/rebuild-acls-day1-restructure.js`, idempotent rebuild + `--dry-run`): **DROP** L4 Technology Review (Ben covers monitors/documentation separately for AHA audit) + the 3 infant-BLS skills blocks (Infant BLS / CPR Skills Test / Choking) — **KEEP BLS Exam** (separate BLS class, same format). **ADD** L-SA Systematic Approach (after L1; L1 relabeled "Overview + Science"), "Ryan Intro (temp)". **Didactic now separate from stations** (stations = lab-rotation time ONLY): L8 split into Cardiac Arrest **Didactic** (videos+disc) + Cardiac **STATION** (4×15=60); Brady/Tachy split into **L5 Brady Didactic + L6 Tachy Didactic** (separate lessons) + **Pre-Arrest Combined STATION** (the existing 4-rotation brady+tachy lab; sec3 relabeled "Pre-Arrest (Combined)"). **LUNCH hard-pinned 12:00-13:00 both days.** ACS + Acute Stroke → 30-min 3-part videos moved before lunch but **compressed into 11:30-12:00** (with L7 HP Teams) to hit the noon pin — two full 44-min morning stations (BLS+Airway) leave no room for full ACS/Stroke/L7 pre-lunch; run full-length live, lunch absorbs spillover (Ben: "manage the squeeze live"). Day 1 now ends **16:25** (stations = rotation time, no longer bundled). **Day 2:** megacode blocks relabeled **NO video** (L9 = live instructor demo Lesson 9B ≤10 min + discussion + rotations; Testing scored, no video); added noon lunch (Practice→Break→Lunch→Testing→Exam, Day 2 ends 15:15). Lab blocks re-linked to their sections; section FRAMES re-synced (BLS 09:45-10:30, Airway 10:45-11:30, Cardiac 13:30-14:30, Pre-Arrest 15:30-16:10, D2 Practice 08:30-11:30, D2 Testing 13:00-14:15). Old block-level instructor links removed (Ben/Rae/Ryan assign people live via the webapp); per-STATION assignments untouched. Revert floor: commit `903f6357`.

- `903f6357` | **RESTORE ACS & Acute Stroke Day-1 blocks (reverses the 2025 "drop")**. Cowork's earlier "drop ACS/Stroke" call came from the wrong condensed *ACLS+BLS-card* variant (p266); the full **"Agenda for ACLS Course" (p66)** KEEPS ACS and Acute Stroke — matching group-13. Correction to `e8ac3866`/`c5f333aa`, which had *deleted* (not archived) the L10/L11 blocks in the wholesale block rebuild — so this is a re-insert, not an un-archive (there was no archived flag and never any ACS/Stroke stations/cert scenarios; the two ACS/Stroke rows in the bank are unrelated standard cases — "Cerebellar Stroke", "Abdominal *Compartment* Syndrome"). Surgical (`scripts/restore-acs-stroke-blocks.js`, idempotent guard + `--dry-run`): re-inserted **ACS (12:10–12:25)** + **Acute Stroke (12:25–12:40)** as Day-1 learning blocks after L3 Airway / before L4 Technology Review (2025 p66 order), kept **Lunch a full hour (12:55–13:55)** per Ben, and shifted the afternoon (+35 min, lab DURATIONS unchanged) → Brady/Tachy 13:55–15:55, Cardiac 16:40–19:20, **Day 1 ends 19:35**. Brady/Tachy + Cardiac **section frames re-synced** to the shifted block times; Day-1 sort_order renumbered by start_time. Block links/sections/stations untouched. Day 2 unchanged.

- `903f6357` | **ACLS Hub schedule print view — clean instructor handout**. The hub's Print button dumped the ENTIRE on-screen dashboard (megacode TL stats, the "not yet passed as TL" failure marker **exposing student names**, per-group coverage, by-instructor) in screen card/colors with no page layout — only the controls were `print:hidden`. Replaced the bare print header in `app/labs/acls-hub/page.tsx` with a dedicated **`hidden print:block` schedule sheet**: per-day B&W tables of Time | Lesson/Activity | Room/Instructor (from schedule blocks) + each lab section's station plan (# | Room | Case/Skill | Instructor), both days regardless of the on-screen day toggle, with `@media print` page setup (letter portrait, 0.5in margins, bordered tables, per-day page break). Wrapped the on-screen dashboard in `print:hidden` so the stats/coverage/by-instructor — and the **student-name privacy leak** — no longer print. tsc 0 + clean build green.

## 2026-06-17

- `4e3fb58c` | **BLS/Airway restructure: Adult BLS + Peds BLS + standalone Airway (all external-graded)**. Replaced the earlier generic 2-station BLS. Airway Management stays its OWN section (sec5, program treats airway as its own class); the BLS section (sec4) now has 2 explicit stations: **Adult BLS** (adult CPR/AED, BVM as part of adult CPR) and **Peds BLS** (infant/peds CPR + relief of choking, BVM as part of infant CPR). All three: schedule visibility + per-station instructor assignment ONLY. **DOC NOTE (on record):** infant/pediatric BLS, adult BLS, choking, and airway are graded on the SEPARATE BLS skill sheet (+ BLS Instructor Manual) via the program's semester-long BLS class — the ACLS app does NOT account for BLS/airway grading (station_notes on all 3 say so; they're station_type='skills', not in the adv_cert tables, so no duplication/double-grade).

- `7bdadc59` | **Aligned ACLS section frames to the 2025 schedule (rotation params UNCHANGED)**. Verified the 2025 block rebuild did NOT compress the labs — section rotation params are intact (Megacode Practice 6×15 'every student leads as TL', Cardiac 4×15, Brady/Tachy 4×10, BLS 2×22, Testing 1×10). Day-2 ~13:40 end is the 2025 agenda's tighter didactic timeline, not lab compression. Fixed a consistency gap: the section FRAME start/end still showed yesterday's clock while the blocks were retimed to 2025 — aligned the frames (Cardiac 16:05-18:45, Brady/Tachy 13:20-15:20, BLS 10:40-11:25, Megacode Practice 08:30-11:25, Testing 11:40-12:55) so each section's 'when' matches its 2025 block. Rotation/station params untouched; every section now has equal-or-more room (no compression).

- `e8ac3866` | **2025 reconciliation decisions applied (Airway station, BLS external-grade, ACS/Stroke confirmed dropped)**. Follow-up to the 2025 agenda rebuild (c5f333aa). (1) **Airway Management** — new Day-1 section (sec5, 11:25-12:10) with one skills station (BVM + OPA/NPA, 1-min timed); the L3 Airway schedule block is linked to it. Schedule-visible + instructor-assignable; checklist graded on the SEPARATE skill sheet, NOT ACLS-internal (station_notes says so). (2) **BLS** — the 2 BLS skills stations annotated as graded on the existing BLS skill sheet (separate BLS class), not ACLS-internal; they are station_type='skills' (NOT in the adv_cert tables), so no double-grade. (3) **ACS & Acute Stroke** — confirmed already dropped: the L10/L11 blocks were removed in the rebuild and there are NO ACLS ACS/Stroke stations or cert scenarios (the two ACS/stroke rows in the bank are unrelated standard cases, left untouched). Lesson order matches 2025 (L1-L9). acls_day_template.json captured for the future repeatable day-builder (Stage 6).

- `c5f333aa` | **Re-timed G14 ACLS schedule to the 2025 agenda** (timing/sequence only; content unchanged). Rebuilt Day-1 + Day-2 `pmi_schedule_blocks` to the 2025 structure (script `rebuild-acls-2025-agenda.js`): Day 1 now Start/Welcome→L1→Infant BLS/CPR Test/Choking→BLS Exam→L2 High-Quality BLS→L3 Airway→L4 Tech→Lunch→L5/6 Brady/Tachy (swap 14:20)→L7 Teams→L8 Cardiac Arrest, end 18:45; Day 2 L9 Megacode Practice→Testing→Exam, end 13:40. Dropped the 2020-only items (Trad 2/3/4, CPR Coach, ACS, Stroke); added the infant-BLS sequence; merged Brady+Tachy. Lab blocks re-linked to their sections (BLS sec4, Brady/Tachy sec3, Cardiac sec2, Practice sec2, Testing sec3). Note: the old blocks' block-level instructor links (pmi_block_instructors, 19) were removed in the rebuild; per-STATION assignments untouched.

- `62bac3bc` | **Imported final enriched ACLS content — all 20 cases, 59 phases (structured), by case_code**. Imported `pmi_acls_cards_converted_ROSCfilled.json` into the existing cert records: **0 new / 20 updated, 20 with narrative, 59 phases** (no standard-bank pollution; cert tags + Paramedic-only + segments all intact). Importer normalizes the converter's shapes to what the display expects: top-level `vitals`→`initial_vitals`, and each phase `{title, presentation_text, instructor_cues[]}` → canonical `{name, presentation_notes, expected_actions[]}` (phase `vitals` keys already matched); also maps `medications_to_administer`. This fixes BOTH the megacode phase display AND the learning-station content (CASE_34/36/39/40, CASE_16/17/26/27 now have presentation + vitals + phases). **Bug fixed during verification:** CASE_48–51 had a **duplicate `team_leader` segment** (my earlier manual add at seq 0 + the v2 seed's seq 1) — deduped to one each. Idempotent — re-run to patch tomorrow's flagged items (OCR-draft narrative, CASE_52 ROSC, qualitative ROSC vitals — all non-blocking per the converter flags).

- `5718ae0c` | **Converter schema + importer ready for full structured ACLS content (all 20, by case_code)**. New `docs/ACLS_CONVERTER_SCHEMA.md`: the exact target JSON the user's scenario converter emits — flat fields matching the `scenarios` columns + the precise nested JSONB shapes (`phases[]` with per-phase `vitals{bp,hr,rr,spo2,temp,bgl,gcs,etco2,ekg_rhythm}` + `expected_actions[]`; `initial_vitals`, `ekg_findings{rhythm,rate,interpretation,twelve_lead}`, `opqrst`, `sample_history{signs_symptoms,last_oral_intake,events_leading}`, `secondary_survey{head…extremities}`), primary assessment `assessment_x/a/b/c/d/e`, text[] (`critical_actions`/`medical_history`/etc.). Importer (`scripts/import-acls-seed.js`) extended to map the primary-assessment text fields, and its UPDATE now **COALESCEs** structural tags so a CONTENT-ONLY seed (case_code + content) never nulls existing identity/tier/segments — imports cleanly into the existing cert records, out of the standard bank. Verified: dry-run still 20/20 narrative. Once the converter delivers all 20 → import fixes BOTH megacode phases AND learning-station content with the full structured display.

- `af1ef12e` | **FIX (Day-1 critical): learning-station scenario content now shows**. Learning stations (CASE_34/36/39/40 etc.) showed blank — diagnosed as a **DISPLAY gap, not a seed gap** (all 8 learning scenarios HAVE content). The cards' lead-in/narrative live in `patient_presentation`/`history`, but (a) the standard grader's scenario embed (`/api/lab-management/stations/[id]`) didn't SELECT those fields and (b) `ScenarioFullDisplay` didn't RENDER them. Fixes: added `patient_presentation, history, environment_notes, avpu, gcs, pupils, assessment_b/c/d, vitals, ekg_findings, secondary_survey` to the station embed; added guarded **Scenario (presentation)** + **Case Narrative (history)** blocks to `ScenarioFullDisplay` (render only when present → auto-collapse, all consumers benefit). Removed the now-redundant free-text from the adv-cert grader header. **Data:** the Brady/Tachy learning stations were unassigned placeholders → assigned the brady/tachy learning cases CASE_16/17/26/27 (all have content). Net: cardiac-arrest + brady/tachy learning stations show their scenario in the standard structured view; minimal grading unchanged. tsc 0 + build green.

- `77c55d54` | **Megacode grader uses the standard ScenarioFullDisplay + collapse-empty**. Replaced the megacode grader's dense text panel with the **same `ScenarioFullDisplay` component the regular scenarios use** (structured familiar layout: read-first instructor notes, vitals strip, patient info, secondary assessment, phases, critical actions). The grader fetches the full scenario row (`/api/lab-management/scenarios/[id]`) for it. Added a **`hideEmpty` prop** to `ScenarioFullDisplay` (default false → existing consumers unchanged) that hides the 3 always-rendering sections (Patient Info, Secondary Assessment, SAMPLE) when they have no data — so sparse OCR'd ACLS cards show only their populated fields cleanly; fully-authored scenarios still show everything. The OCR free-text the structured component has no home for (lead-in→`patient_presentation`, narrative→`history`) is surfaced in the grader's case header (lead-in line + collapsible narrative) so nothing is lost. Bounded: no layout redesign — standard format, just collapse empties + surface the free-text. tsc 0 + build green.

- `b6941a9d` | **Grader back-to-context (FIX 1) + BLS section**. FIX 1: the adv-cert grader's Back link went to `/labs` regardless of context; now when you arrive from a station's "Grade (ACLS)" (has `labDayId`) it returns to **that lab day** (`/labs/schedule/[labDayId]`, "Back to Lab Day") — kills the per-rotation friction. BLS section (`scripts/build-bls-section.js`, idempotent): new Day-1 **BLS** section (sec 4, 10:15–11:00, the BLS block) with **2 skills stations** — Adult CPR/AED (combined CPR + bag-mask + AED) and Peds/Infant Choking (combined infant + child); class split in half (2 groups/2 stations). `station_type='skills'`, no skill_sheet → minimal pass/fail via the generic **non-blocking** grader (no rubric, not the focus). Instructors assigned per-station via the Edit Station modal (left blank). Re-linked the Day-1 "High-Quality BLS (L6)" block to the new section. Shows on the ACLS Hub like the other sections; NOT a megacode (excluded from TL stats). Combined-station verification follows AHA intent (Cowork-verified). **FIX 2 (hub "Open" → station page) deferred** — ambiguous (Open already opens the lab-day detail listing the stations); needs a quick clarification on the intended target. tsc 0 + build green.

- `7b951cac` | **ACLS narrative imported (card format) + Phase 2 grader display + OOH testing set 1/2/3/4**. Cowork's OCR'd seed stored narrative under a `card` block (`lead_in`/`vital_signs`/`narrative[]`/`instructor_notes[]`) — not the flat schema — so the importer (`scripts/import-acls-seed.js`) was extended to map `card.*` onto our columns (lead_in→patient_presentation, narrative→history, instructor_notes→instructor_notes, vital_signs→initial_vitals + lifts Age→patient_age/Weight→patient_weight, setting→environment_notes). Imported: **20/20 ACLS scenarios now have narrative** + 2 new testing cases (TEST_1/TEST_3). **Phase 2:** `getScenarioWithSegments` + `AdvCertScenario` now carry the narrative fields, and the adv-cert grader renders a **case panel** (scenario stem, age/sex, vitals chips, collapsible narrative + instructor notes) above the criteria — the familiar "scenario on top, grade below" format. **Testing set → OOH 1/2/3/4:** instead of re-running `build-acls-days.js` with the updated schedule seed (which would clobber the re-linked/retimed blocks), surgically swapped the Megacode Testing section's stations TEST_9→TEST_1, TEST_10→TEST_3 (`scripts/swap-acls-testing-cases.js`) and deactivated the orphaned 9/10. tsc 0 + build green. (3 reframed lead-ins CASE_39/40/27 are wording_draft pending sign-off — re-import to patch.)

- `40daa5f9` | **Fix stale ACLS block references — re-link blocks to real sections + suppress monolith on /calendar**. Root cause of the "stale Day-2 morning block": the ACLS lab schedule blocks were created (pre-sections) linked to the monolithic section-1 lab_day, and one title still referenced the old case set. Data fix (`scripts/relink-acls-blocks-to-sections.js`, idempotent/dry-run): re-linked 8 lab blocks to their real sections — Day-1 Cardiac Arrest→sec2, Brady/Tachy→sec3; Day-2 Megacode Practice (intro/main/extra)→sec2, Megacode Testing→sec3 (the 3 non-sectioned Day-1 blocks BLS/ACS/Stroke keep section-1) — and retitled "Megacode Practice (Cases 16/17/26/27 → 48-55)" → "(Cases 48–51)". Code: `app/api/calendar/unified/route.ts` now also hides the monolithic section-1 lab_day on sectioned dates (mirrors the hub), so re-linking doesn't leave it as a standalone duplicate on the main calendar. Net: blocks point at the current sections, no stale case text, no monolith duplicate anywhere. tsc 0 + build green.

- `a9b0290e` | **ACLS grader Phase 1 (auto-select + case header) + importer narrative mapping (Path A) + seed schema**. (1) **Phase 1** in `app/labs/adv-cert/grade/page.tsx`: arriving from a station's "Grade (ACLS)" now **auto-selects that station's drawn scenario** (no more re-picking the case every rotation), and a **case-identity header** renders above the criteria (case_code, title, tier badge, and the segment "case flow") — useful immediately for megacode even before narrative exists. Revertable; floor was `bae58efa`. (2) **Importer Path A** (`scripts/import-acls-seed.js`): now maps narrative case fields (text / int / text[] / jsonb) from the seed onto the `scenarios` row — additive, idempotent, a no-op on the old structural-only seed (verified: 0 narrative on existing seed). This is the one-time change so Cowork's OCR'd seed loads real case content (lead-in, vitals, phases, etc.). (3) New `docs/ACLS_SEED_SCHEMA.md`: exact field names/types/formats (extracted from a populated scenario) for Cowork to author the 12 megacode cases' narrative. Once that seed lands + imports, grader Phase 2 shows the real cases. tsc 0 + build green.

- `7486b766` | **ACLS Hub — hide the monolithic section-1 fallback** when the date is sectioned. `app/labs/acls-hub/page.tsx`: a new `visibleLabDays` memo drops any `section_number=1` lab_day on a date that also has real sections (2+), so the old 16-station "ACLS Certification — Day N" block no longer clutters/duplicates the real sections on the hub. It stays in the DB as the grading fallback — only hidden from this view; a date with only section 1 still shows. Applied across the hub's sections list, by-instructor view, and megacode stat scope. Build green; tsc clean.

- `d6b24588` | **Student-notification blackout on ACLS days + main-page ACLS Hub card**. (1) New `lab_days.suppress_student_emails` flag (migration `20260617_lab_days_suppress_student_emails.sql`); set true on the 6 G14 ACLS lab_days (06-18/06-19). Enforcement covers **ALL student-facing send paths, date-wide**: `lib/email.sendEmail` (the chokepoint every specific sender funnels through) blocks any email to a student address (`@my.pmi.edu`) when today has a flagged lab_day — all templates, not just eval; and `lib/notifications.createNotification` + `createBulkNotifications` skip student recipients on those dates (in-app). Mirrors the existing NREMT date-guard pattern but **fails OPEN** (a DB hiccup never silently suppresses everything). Instructors/staff (@pmi.edu) unaffected; auto-scoped to the flagged dates (no global toggle to remember). (2) Added a prominent red **ACLS Hub card** to the main page (`app/page.tsx`, instructor+) linking `/labs/acls-hub` — the discoverable entry point (the calendar tab wasn't workable). Schema doc updated. tsc 0 errors + build green.

- `896253ca` | **ACLS grading wiring — route megacode to the ACLS rubric, picker sees the ACLS bank, non-megacode non-blocking, +B/EMT-strip**. Root cause was that the standard lab-station UI ignored the (correctly-set) `cert_course` tag, so ACLS sections fell back to the standard bank + standard 0–4 rubric. Fixes: (1) **StationCards Grade button** now routes to the adv-cert ACLS grader (`/labs/adv-cert/grade?labDayId=&stationId=`, red "Grade (ACLS)") when the lab day is a **megacode** section (`is_adv_cert_testing` or section label "megacode"); non-megacode keeps the standard grader. (1b) The adv-cert grade page **preselects** day+station from the URL. (2) **EditStationModal** pulls the cert bank (`includeCert`, scoped to the day's `cert_course`) on megacode sections, so ACLS scenarios are selectable and an assigned one renders instead of blank. (3) **Non-blocking generic grader** (`/labs/grade/station/[id]`): the "rate all criteria" + NI/U-justification gates now apply **only to strict NREMT (E204)**; the generic/formative sheet lets an instructor flag a concern (flagging fully kept, incl. affective/behavioral) and submit without being blocked. Data (via `scripts/fix-acls-grading-data.js`, applied): **B** — added the `team_leader` segment to practice scenarios CASE_48–51 (now 6-segment testing-style, captures the TL eval; banks stay separate); **EMT-strip** — removed EMT/AEMT from all 20 cert scenarios' `applicable_programs` (now Paramedic-only) to stop the EMT leak. Cardiac-arrest cases left learning-graded per decision. tsc 0 errors + build green (verified per the deploy lesson).

- `d8842e5f` | **ACLS Hub — megacode-only TL scope + failure marker; Megacode Practice → 6×15**. (1) Data: Megacode Practice set to **6 rotations × 15 min** (= 90 min active vs 135-min block, +45 slack) so every student leads once — intentional, no flag. (2) ACLS Hub (`app/labs/acls-hub/page.tsx`) now scopes team-lead stats to **MEGACODE ONLY** (practice + final testing sections; identified by `is_adv_cert_testing` or section label "megacode"), per the rule that ACLS passing is about megacode TL experience — brady/tachy + cardiac-arrest learning are excluded here (they stay in the semester/course overview, which already counts ALL team-leads via `team_lead_log` + `scenario_assessments`). "Passed as TL" combines **practice + testing** megacode passes. Added a **failure marker** section: "Not yet passed megacode as TL" listing students, red = attempted+failed, gray = not-yet — the AHA every-student-leads goal made visible. Group chips + per-group icons rescoped to megacode. Build green; tsc clean (clean check per the deploy lesson). **HELD pending decision:** grading practice with the 6-segment *testing* grade sheet (see notes — the grade sheet is per-scenario segments; needs an A/B pick on mechanism).
- `b8b0a38b` | **FIX: Vercel deploy blocker — `section_number` type error** (un-blocked 9 un-deployed commits). `app/labs/schedule/page.tsx:1122` `(labDay.section_number ?? 1) > 1` failed Vercel's clean type-check ("Operator '>' cannot be applied to types '{}' and 'number'") because `labDays` comes from the `useLabDays` hook whose `LabDay` interface has an index signature `[key: string]: unknown` — `section_number` fell through it as `unknown`, so `unknown ?? 1` widened to `{} | number`. Fixed by adding `section_number`/`section_label` **explicitly** to the hook's interface (`hooks/useLabDays.ts`), the same pattern already used for `priority_flag`. Root cause of the local-passes/Vercel-fails split: local `next build` reused the `.next` TypeScript cache; Vercel builds clean. Verified with a fresh `tsc --noEmit` (0 errors) AND a `rm -rf .next` full build (exit 0). Every deploy since `d1980d80` (inc 3) had been failing, leaving the multi-section UI + ACLS Hub in the repo but not on the live site (DB changes were applied directly, so the database was already correct).

- `2f44daf4` | **ACLS section rotation corrections (2025 program-tuned)**. Script `scripts/fix-g14-acls-rotations.js` (transactional, dependency-checked, dry-run capable): Cardiac Arrest (D1 sec2) → 15 min/rot (10 station + 5 debrief) × 4 rotations × 4 stations (filled the previously-blank gap); Megacode Practice (D2 sec2) → 15 min/rot × **4 rotations × 4 stations** (was 2×8 — trimmed CASE_48–51 kept, CASE_52–55 removed after a hard FK-dependency check confirmed no grading/assignment rows). Standard is now **4 rotations × 4 stations** for all labs (every student must lead); TESTING is the only legitimate exception. Compute-and-flag math: Cardiac 4×15=60 ≤ 120-min block (+60 buffer); Practice 4×15=60 ≤ 135-min block (+75 buffer, plus the extra-practice block). **HELD:** Megacode Testing (D2 sec3) untouched pending instructor-book confirmation (may be 3 scenarios / pass 2-of-3). **FLAGGED:** Brady/Tachy (D1 sec3) left at 4×10 — it's 4/4 ✓ but differs from the 15-min standard; at 15 it'd be 60 > its 45-min block, so it needs a decision (leave at 10, or extend the block). Never compressed station length.

- `3c50ad46` | **ACLS Hub — read-only aggregator page** (`/labs/acls-hub`). Consolidates the scattered ACLS surfaces (planner/calendar/lab-day/coordinator) into one accessible section for the 7–10 instructors (incl. RT guests) running the event. New read-only endpoint `app/api/adv-cert/acls-hub/route.ts` gathers a cohort's ACLS lab_days across **all sections + both days** (+ stations, cohort groups/members, and scored attempts across all those lab_days — auto-detects the nearest ACLS cohort when no `cohortId` given). New page `app/labs/acls-hub/page.tsx`: Day 1/Day 2 legible schedule (didactic + labs together, from the unified aggregator), per-day lab sections with Open/Assign/Tracker links, **coordinator stats AGGREGATED across both days & all sections** (the fix for the old single-lab-day coordinator scope — confirmed: `grading-context`/`attempts` are single-`labDayId`, so post-sections the old view showed one SECTION), whole-event team-lead coverage per group, by-instructor station list, and a clean print view. Nav: red "ACLS Hub" button on `/calendar`. Writes nothing; existing surfaces remain the fallback. Build green. SITEMAP updated.
- `f4471973` | **Stage 2b — G14 ACLS sections built (additive data)**. New idempotent script `scripts/build-g14-acls-sections.js` created 4 section lab_days alongside the untouched section-1 monolithic days, so instructors can be assigned per-station-per-section: Day 1 (06-18) sec 2 **Cardiac Arrest** (14:00–16:00, 4 stations CASE_34/36/39/40, per-station duration left as a GAP for the user), sec 3 **Brady/Tachy** (16:30–17:15, 4 stations × 10 min); Day 2 (06-19) sec 2 **Megacode Practice** (09:45–12:00, 8 stations CASE_48–55, 2 rotations × 25 min — buffer preserved as the "extra practice if needed" slack), sec 3 **Megacode Testing** (14:00–15:00, individual_testing/scored, 4 stations × 10 min MEGACODE_TEST_2/4/9/10). All on the user's current DB block times. Instructors intentionally left BLANK (user assigns via Edit Station modal = the station_instructors path that syncs to Google Calendar). CASE_16/17/26/27 (Day-2 morning scenarios) left in section 1 only. Verified: section 1 (4 + 16 stations) and the 30 schedule blocks untouched. Revert: delete the 4 new lab_days (cascades stations); section 1 remains the fallback.
- `d1980d80` | **Stage 2 increment 3 — section-aware code + UI** (completes the Stage 1a change-list; the operator can now run/grade/track multiple lab sections per cohort per date). **API:** `lab-days` POST **auto-assigns the next free section** for a (date, cohort) — creating a second lab day on the same day just becomes section 2 (no collision), and accepts an explicit `section_number`/`section_label`; GET returns the section fields. **Calendar dedup (Stage 1a #2):** `unified` + `feed.ics` now dedup on `(date, cohort, section)` — a lab block only suppresses its own section (block section = `COALESCE(linked_section_number, 1)`), so section 2+ stays visible; event titles get a `· Section N`/label tag for section 2+. **Planner (Stage 1a #5):** the block modal's lab-day lookup is now scoped by cohort and prefers the block's FK link / target section instead of `labDays[0]`. **Day-builder (Stage 1a #3):** `build-acls-days.js` scoped to `section_number=1` explicitly. **UI:** section badge on the lab schedule list + grid, and a section banner on the lab-day detail page (shown only for section 2+, so single-section days read unchanged). Types updated (`PmiScheduleBlock.linked_section_number`, `LabDay.section_*`). Verified no singular "find THE lab day" read throws on multiple sections. Build green. **G14's ACLS day is section 1 and unaffected.**
- `befdd390` | **Stage 2 increment 2 — section-aware calendar link triggers** (Stage 1a highest-risk spot #1). Migration `20260617_calendar_link_triggers_section.sql` rewrites both link functions (`pmi_link_block_on_lab_day_insert`, `pmi_link_lab_day_on_block_publish`) to match a block↔lab_day on `(cohort, date, section)` instead of just `(cohort, date)` — block target section = `COALESCE(linked_section_number, 1)`, so a section-2 lab_day only links to a block explicitly targeting section 2, and a block with no section links to section 1. Added a deterministic `ORDER BY … LIMIT 1` so a duplicate can never fan a time-copy across rows. `CREATE OR REPLACE` keeps the existing triggers attached. **Unchanged for all current single-section data** (everything is section 1). Verified: both triggers attached + functions section-aware. Revertable: re-apply `20260429_calendar_link_triggers.sql` to restore the prior functions.
- `4382e036` | **Stage 2 increment 1 — lab_days section constraint** (multi-section labs per cohort per date; foundation for grading/tracking across sections). Migration `20260617_lab_days_sections.sql`: adds `lab_days.section_number INT NOT NULL DEFAULT 1` + `section_label`, replaces `UNIQUE(date, cohort_id)` with the **superset** `UNIQUE(date, cohort_id, section_number)`, adds `pmi_schedule_blocks.linked_section_number`, + supporting index. **Purely additive — no data moves; every existing lab_day (incl. the live G14 ACLS Day 1/Day 2) becomes section 1 and grades identically.** Backward-compatible on its own (no code references the columns yet). Schema doc updated same commit. FK-ambiguity check clean. Revertable: this is a DB-only change; reverting the constraint is a follow-up migration, but nothing breaks while it's in place.
- `ab95a0b0` | **ACLS Coordinator Tracker — print view + auto-refresh** (Thursday-relevant). Added a print-only **team-lead tracking sheet** (paper backup for the ACLS course, complements the Google-forms fallback): per-group tables with Student / Team-led? / Scenario / Pass-Fail / Notes columns; students already recorded in the grader are pre-checked (✓) at print time, blanks for handwriting during the day. Screen UI hidden on print, sheet hidden on screen (`print:` variants). Also an **auto-refresh** toggle (Off/30s/60s) + "updated HH:MM:SS" timestamp so the live view tracks grading progress hands-free. One file (`app/labs/schedule/[id]/acls-coordinator/page.tsx`); additive/safe. Build green. **Fallback floor for the Stage 2 multi-section work that follows.**

## 2026-06-15

- `b061f800` | **Student progress — group aggregate** (viewing page #3, privacy-scoped). The individual student progress page already existed (`app/student/my-progress`); added the group-comparison half. New `app/api/student/group-stats/route.ts` resolves the authenticated student → their own lab group (membership-gated; a student can't query another group) and returns **aggregates only** (member count + averages for scenarios, scenario score, skills, team-lead) — never other students' names or rows. The my-progress page now shows a "You vs <group> (averages only)" panel. Privacy: individual classmates' progress stays private; only group averages are exposed. Build green; additive/safe.
- `bc683909` | **Cohort Progress** (viewing page #2) — mid-semester tracked stats for a cohort. New `app/academics/cohorts/[id]/progress/page.tsx`: summary (students, lab days, avg scenario score /4, skill completion %, team-lead rotations + students-with-zero), at-risk flagged list, and a per-student table (scenarios/avg-score/skills/team-lead). **Reuses the proven `/api/reports/lab-progress?cohortId=` endpoint** (no new aggregator — proven columns), so it's a pure display layer. Nav: "Progress" ToolCard on the cohort hub. Build green; additive/safe.
- `43b32aad` | **ACLS Coordinator Tracker** (viewing page #1) — read-only stats + plan-state on the lab day, for ACLS/PALS days (distinct from the NREMT operational dispatch coordinator). New `app/labs/schedule/[id]/acls-coordinator/page.tsx`: summary stats (groups/tested/passed/failed/pending/team-led coverage), a "who's teaching" station plan-state panel, and per-group status + what's-next + per-member team-lead coverage chips. Pure aggregator over existing `/api/adv-cert/grading-context` + `/api/adv-cert/attempts` (no new data). Extended `grading-context` stations select with `instructor_name/room/custom_title/station_notes` (additive) for the plan-state view. Nav: red "ACLS Tracker" button on the lab day page when `is_adv_cert_testing`/`cert_course` set. Added `is_adv_cert_testing`/`cert_course` to the lab-day TS type. Build green; additive/safe.
- `7fb2b0a3` | Planning Workspace — **room assignment per block** (completes the conflict-resolution loop). The room shown on each card is now a clickable inline picker (from `/api/scheduling/planner/rooms`) → assigns/clears `room_id` via the block PUT; the change immediately feeds the live room-conflict detection so you can detect a double-book and reassign a room to clear it, all in the workspace. Mirrors the instructor picker; drag disabled while the room picker is open. One file (`app/scheduling/workspace/page.tsx`); additive/safe. Build green.
- `d6c855a1` | Planner — **live conflict highlighting** (mirror of the workspace). Added the same client-side instructor + room time-overlap detection to the semester planner: conflicting `TimeGridBlock` tiles now get a red ring, recomputed live on every drag/edit — a live complement to the existing static precomputed `ConflictBanner`. `liveConflictIds` memo over the loaded blocks; new optional `conflict` prop on `TimeGridBlock`. Additive/safe, one file (`app/scheduling/planner/page.tsx`). Build green.
- `aa32811e` | Planning Workspace — **add / remove / rename blocks in-place** (now a complete standalone scheduling tool: add, remove, drag, assign, publish, all in one surface). Per day column: **+ Add block** creates a DRAFT block appended after the day's last (status defaults to draft → off the live calendar until publish) via POST `/blocks`. Each card: **✕** removes it (confirm) then tetris-cascades the day to close the gap; **click the title** to rename inline (PUT title/course_name). Drag is disabled while a title/instructor picker is open. Reuses existing block POST/PUT/DELETE; one file (`app/scheduling/workspace/page.tsx`). Build green; additive/safe.
- `7acb257a` | EditStationModal — add per-station **rotation-time editing** (closes Bug 2's real remainder). The modal had no inputs for `rotation_minutes` / `num_rotations`, so per-station timing couldn't be edited even though the stations PATCH already persists them. Added "Minutes per rotation" + "# of rotations" number inputs (populated from the station, sent in the existing PATCH). Additive/safe, single file. Build green.
- `461bb583` | Calendar **Phase 4 (partial, safe slice)** — external ICS feed now matches the in-app unified calendar. Extended `app/api/calendar/feed.ics` (the token-gated subscribe feed) to emit the 6 previously-missing unified sources — LVFR placements (published only), clinical site visits, open shifts (non-lab-coverage), ride-alongs, exam sessions, volunteer events — alongside the blocks + lab days it already had. Additive to one read-only file (its established inline pattern); core unified route untouched. `X-Feed-Extra-Count` header added. **Deferred to a focused/branch session (flagged careful):** the full consolidation — extract one shared aggregation lib for the unified route + feed.ics + export-ics, and **retire the redundant `planner/ical/[semesterId]`** route (auth-gated so it never worked as an external subscribe; linked from 2 planner pages). That's a refactor of the working calendar read-path, not safe/additive, so per the build discipline it gets focus/a branch rather than a rushed end-of-session move. Build green.
- `4ffc8ed2` | Planning Workspace increment 4+5 — **live conflict highlighting + instructor assignment** (additive/safe). Conflicts: client-side **live** detection (same instructor on time-overlapping blocks on the same date, across ALL cohorts in the loaded week) — recomputes on every drag so conflicts show/clear as you arrange; conflicted cards get a red ring + ⚠, header shows an "N instructor conflicts" pill. Chose live client-side over the planner's static precomputed `pmi_schedule_conflicts` table because drag-to-arrange needs immediate feedback (the precomputed table could be layered in later for room/other conflict types). Instructor assignment: per-card person chip → inline `<select>` (from `/api/scheduling/planner/instructors`) assigns/clears the primary instructor via the block PUT `instructor_ids` (optimistic + reload); drag is disabled while a picker is open. No new endpoints; one file (`app/scheduling/workspace/page.tsx`). Build green.
- `685c40f9` | Planning Workspace (Stage 4b) — **rough-testable** drag-to-arrange + publish over `pmi_schedule_blocks` (3 increments). New `/scheduling/workspace`: semester/cohort/week selectors → day-column render of draft+published blocks; **drag a block** within/between days → the day re-cascades (tetris: re-lay from the day anchor, each block starts where the prev ended, lengths preserved) → persists start/end/date/sort via the block PUT (optimistic + reload); **Publish N drafts** flips the cohort's visible-week drafts to published via an extended `publish-all` (now accepts optional `program_schedule_id` + `date_from`/`date_to` for scoped publishing — backward compatible). "All cohorts" is view-only; a cohort must be selected to edit. Nav: "Planning Workspace" button on `/calendar` (surfaced as a calendar view per the master sequence). Safe/additive — drafts don't hit the live calendar until published; no constraint work. Builds green. Commits: `938828e1` (render), `69afd890` (drag+cascade+save), this (publish+nav).
- `f0f496e0` | Calendar unification **Phase 2** — filterable view set on `/calendar` (safe/additive, no constraint work). Replaced the 3 ad-hoc presets (All/Instructor/Labs) with a config-driven set: **Full Master · Lab-only · Cohort · Medic-only · Didactic-only · By Instructor**. New `applyPreset()` sets program + event-type filters together per view (Lab-only→labs; Medic-only→paramedic; Didactic-only→class+exam; Full Master→all) — and **fixes a latent bug** where "All" left `activeEventTypes` stuck at {lab} after Labs Only. Added a **Cohort** view with a cohort dropdown (`/api/lab-management/cohorts?activeOnly=true`) that passes `cohort_id` to the unified endpoint (scopes classes/labs/clinical/ride-along to one cohort; uncohorted exams/volunteers hidden in that view, as designed). Builds on Phase 1's complete aggregator. Build green. Independent of the held Stage 2 constraint work.
- `04c29c9a` | docs: **Stage 1a** dependency map (read-only) for the `lab_days` UNIQUE(date,cohort_id) constraint change, in `CALENDAR_ARCHITECTURE.md`. Highest-risk spots (calendar link triggers, unified-calendar dedup, `build-acls-days.js`, template apply, planner modal `labDays[0]`) + full categorized change-list + recommended `section_number` identifier + new UNIQUE(date,cohort_id,section_number). Gates Stage 2. **Thursday guardrail recorded:** Stage 2 (constraint reshape) + 2b (migrate live G14 day) HELD until after Thursday's ACLS course (Google-forms fallback covers Thursday); Stage 1a done now.
- `92b7e9ab` | Fix planner instructor-assignment reload bug (edit-save reliability). Instructor assignments WERE saving to `pmi_block_instructors`, but the planner edit form rehydrated from `bi.instructor_id` while the API embed only returned the nested `bi.instructor.id` — so reopening a block showed "unassigned" and looked unsaved. Added `instructor_id` to the `pmi_block_instructors` embed in both planner block selects (`blocks/route.ts` + `blocks/[id]/route.ts`). Also: `buildPayload` now always sends `instructor_ids` (was omitted when empty → couldn't clear the last instructor; PUT handles empty by clearing). Backfilled 2 ACLS blocks whose instructor lived only in the FK column `instructor_id` into the join (planner + unified read the join); `scripts/rebuild-acls-blocks.js` now writes the `pmi_block_instructors` join row alongside the FK so future runs stay consistent. Diagnosis confirmed the time-edit symptom was the same reload illusion (planner block start/end DO persist); the only real time gap is per-station `rotation_minutes`/`num_rotations` having no inputs in EditStationModal (separate small UI add, not done). Build green. Unblocks manual ACLS instructor prep.
- `322be448` | docs: extended `CALENDAR_ARCHITECTURE.md` into the **master sequence** — one dependency-ordered build plan covering the calendar phases + lab-structure constraint correction + team-lead-per-section tracking + planning workspace + repeatable day-builder + instructor-availability conflict-detection. Adopts **correcting the `lab_days` UNIQUE(date,cohort_id)** constraint (high-care, dependency-mapped first) over the sections-in-one-lab-day workaround, per the master-sequence brief (the constraint is a false assumption — multiple real labs/day occur program-wide). Each stage tagged S/M/L + safe/careful; recommended order 1a(map)→2(constraint)→2b(migrate ACLS)→3(team-lead), with calendar views/workspace in parallel. Proposal for sanity-check before execution.
- `338fde08` | Calendar unification **Phase 1** — complete the unified aggregator + architecture doc. Added the two missing stores to `app/api/calendar/unified/route.ts`: `exam_sessions` (self-scheduler written exam — `event_type:'exam'`, links to `/exam-scheduling`) and `volunteer_events` (`event_type:'volunteer'`, new teal color, active-only, links to `/admin/volunteer-events`); both added to the default `include` set so a day with an exam/volunteer event now shows it (previously orphaned from every calendar — "no single view shows a complete day"). `CalendarEvent` source/event_type unions extended; both honor cohort/program filters (uncohorted → hidden when a cohort is selected). Robust per-source try/catch unchanged. Build green. New `docs/CALENDAR_ARCHITECTURE.md` captures the full scoping: data is fragmented across 8 domain stores but a unified READ aggregator already exists (now 8/8) — "one filterable calendar" is a read/display-layer job, NOT a data migration; phased plan with **Phase 3+ (adopt unified on high-use surfaces) deliberately held for post-course**. Phase 0 (planner cohort filter) + Phase 1 done.
- `efa0183b` | ACLS Day-2 flexible buffer relabeled to name the decision point (final schedule-rebuild piece). The 1300–1400 block is now `ACLS LAB · Extra Megacode Practice if needed — else start Testing` (was "Megacode Practice (if needed) — flexible buffer") — honestly describes the run-time choice (skip extra practice → start testing at 1300). Re-ran `scripts/rebuild-acls-blocks.js` (30 blocks; prior set backed up to `g14_acls_blocks_pre_rebuild3_20260615.json`; G5 preserved; stations untouched). Concludes the ACLS schedule-block rebuild.
- `254b1f24` | Planner **cohort-scoping filter** + ACLS Day-2 PM made flexible. (1) Planner calendar (`app/scheduling/planner/page.tsx`) was date/semester-scoped only, so every cohort's blocks on a date rendered together (G14 ACLS visually collided with G5's EMT Lecture). Added a **Cohort filter** dropdown (mirrors the existing room/instructor filters): new `cohortFilter` state, `visibleBlocks` now drops blocks whose `program_schedule→cohort` ≠ the selected cohort, options built from the loaded program schedules (`getProgramLabel`). "All Cohorts" = prior behavior. Build green. (2) Re-ran `scripts/rebuild-acls-blocks.js` with a flexible Day-2 afternoon (30 blocks now, 20 Day1 + 10 Day2): lunch defaulted to **noon (1200–1300)**; Practice ends at noon (1000–1200); a labeled **"Megacode Practice (if needed) — flexible buffer" (1300–1400)** the group often skips; **Megacode Testing 1400–1500**; the written exam relabeled **"AHA Online Written Exam (external — student computers)"** (AHA site, our system doesn't administer it); Remediation 1600–1700; End 1700–1730. Prior 29-block set backed up to `data/acls/backups/g14_acls_blocks_pre_rebuild2_20260615.json`; G5 preserved. Stations untouched.
- `6d015c56` | ACLS schedule blocks rebuilt from the authoritative xlsx Time column (G14). The earlier blocks used the AHA "Start times" reference column (wrong clock → overlaps/gaps/1:05 lunch) and a uniform `course_name='ACLS'` (calendar renders course_name → "wall of ACLS"). New `scripts/rebuild-acls-blocks.js` rebuilds from `ACLS group 13.xlsx` "Time" column (our adapted schedule): backed up the 21 existing G14 ACLS blocks (gitignored `data/acls/backups/g14_acls_blocks_pre_rebuild_20260615.json`), deleted them scoped to G14's `program_schedule_id` (G5's EMT Lecture preserved), wrote **29 split blocks** (20 Day 1 + 9 Day 2). One independently-assignable block per lesson (SPLIT, never pre-combined — for per-cohort instructor reassignment), breaks/lunch now VISIBLE (no mystery gaps), the 17:30 zero-duration debrief fixed. Informative `course_name` labels with lab-vs-lesson distinction (`ACLS · L1 Course Overview`, `ACLS LAB · High-Quality BLS`, `ACLS · Lunch`). Day-2 calls: Practice extended to 1300 (fills the sheet's blank noon slot), Megacode Testing set to 1400–1500 (real 4-station window). Instructors resolved where uniquely matchable (R. Young→Ryan Young, B. Hartnell→Hartnell); M. Schaffer & J. Lomonaco not in lab_users → name kept in content_notes, FK null. Stations (grading layer) deliberately untouched — grading stays verified-ready for Fri. Flagged for post-course: brady/tachy cases appear inside Day-2 practice in the xlsx vs the current station layout (stations not changed). Content version note: loaded grading content is tagged AHA 2020 (no 2025 rows loaded yet). Next: cohort-scoping filter on the planner calendar (it's date/semester-scoped, so G5 shows alongside G14).
- `6075c43b` | Picker filter fix A — hide ACLS cert-tagged scenarios from the normal lab picker. `/api/lab-management/scenarios` now selects `cert_course`/`cert_tier` and **default-excludes `cert_course IS NOT NULL`** (`?includeCert=true` opts in). Fixes ACLS/megacode_testing leakage into normal (ILS/EMT) lab building across all 3 surfaces that share this endpoint (lab-day builder, EditStationModal, clinical summative) in one change. Categorization, not lockout — content stays in the bank; the adv-cert grader (`/api/adv-cert/scenarios`) is unaffected. Verified live: normal picker 103→83 active scenarios, hides all 20 cert-tagged (incl. 4 megacode_testing). Build green. Root-cause investigation (read-only) confirmed the shared cause "pickers don't filter on existing tags"; B (EMT/level default-narrow) and C (full picker unification) scoped as separate deliberate tasks.
- `5bf3a094` | ACLS — 4 megacode TEST cards assembled (data load, no code). Re-ran `scripts/import-acls-seed.js` against the corrected/complete `acls_scenario_seed.json` (now includes a 10th segment `team_leader` + full 6-segment chains for MEGACODE_TEST_2/4/9/10). Idempotent deltas: +1 segment, +2 criteria, +24 assembly rows (4 cards × 6 segs); practice cards' 40 rows + the other 9 segments/41 criteria re-affirmed unchanged. Verified live: all 4 test cards load 6 ordered segments each with criteria, titles present ("Megacode Test N"). The Day-2 scored testing block is now gradeable end-to-end. (Chains transcribed from AHA 2025 IM Module 5 Megacode Testing Checklists 2/4/9/10; the seed flags a build-time confirm vs the live test cards.)
- `c23c26c8` | Role-default rule — roster-driven student auto-provision (decision A1 + B1). Enhances the first-login provisioning in `lib/auth.ts` (`signIn` callback, create-only `if (!existingUser)` branch — **first-provision only; existing roles never auto-changed**). For `@my.pmi.edu` logins: inline roster match on `students.email` (mirrors `getRosterStudent`); on match → role `student` + set `primary_cohort_id` from the matched cohort (B1); on NO match → still `student` (A1 floor — restricted role, safe to auto-grant) + notify admins via new `notifyAdminsUnmatchedStudentSignup()` to review/assign. `@pmi.edu` unchanged (`pending` + admin notified; **never auto-elevated** — one-directional). Guardrails verified: subsequent logins short-circuit (the 3 `volunteer_instructor` accounts on my.pmi emails keep their role); fake/unrostered my.pmi email correctly hits the floor+notify path; real G14 student matches → cohort set. Note: the bare `@my.pmi.edu → student` default already existed; this adds the roster match, cohort enrichment, and unmatched-admin-notify. Data context: active cohorts G13/14/15 have complete my.pmi emails + roster rows (the ~62-login wave matches on both signals). Build green. `docs/ROLE_DEFAULT_APPROACH.md` records the approach/decisions.
- `8e3bb2f5` | Advanced-Cert (ACLS) **day-builder Step 5 complete — G14 teaching/lab blocks (replace)**. Per the "replace those 2 days" decision: removed G14's 8 existing semester-2 program blocks on 2026-06-18/19 (EMS 172/152/192, EMT Lecture[G14], Day 1/2 S2 Lab) — scoped to G14's `program_schedule_id` so cohort **G5's "EMT Lecture" on the same Thu was preserved** (8 of the 9 date-matching blocks were G14's). Backed up the 8 rows to gitignored `data/acls/backups/g14_blocks_pre_replace_20260615.json` before deleting (reversible). Then ran the builder with blocks: **21 ACLS `pmi_schedule_blocks`** (lessons/admin/exam/remediation + the 4 scenario sessions emitted as `lab` blocks `linked_lab_day_id`→the day, so practical sessions show on the calendar), break/lunch omitted, all `status='published'`, `course_name='ACLS'`. Builder refinements: block upsert scoped by `program_schedule_id IS NOT DISTINCT FROM` (never touches another cohort's same-date/title block); scenario_station blocks now also create a calendar block (not just lab_day stations). lab_days (2) + lab_stations (20) re-affirmed. M. Schaffer + B. Young remain unlinked (stored as text). `.gitignore` += `data/acls/backups/`. No app code (standalone script).
- `ac4bc322` | Advanced-Cert (ACLS) **day-builder — G14 lab days populated** (Step 5, lab_days+stations half; teaching blocks held — see below). New reusable `scripts/build-acls-days.js` (`--cohort/--day1/--day2`, idempotent SELECT-then-write, `--dry-run`, `--skip-blocks`) generates a cohort's ACLS days from `acls_schedule_seed.json`. Ran for G14, Day1=2026-06-18 / Day2=2026-06-19 (`--skip-blocks`): enriched the two pre-existing placeholder lab days ("ACLS Certification — Day 1/2", which had 0 scenario_assessments / 0 adv_cert_attempts — no data lost) — Day 1 = 4 scenario stations (CASE_34/36/39/40, 0-4), Day 2 = 16 stations (4 brady/tachy 0-4 + 8 megacode practice CASE_48–55 + 4 testing MEGACODE_TEST_*) flattened into the single lab_day (UNIQUE `(date,cohort_id)`), `is_adv_cert_testing=true` on Day 2, each station tagged with its phase via `station_notes`. scenario_id resolved from case_code; instructor name stored verbatim + FK only on a unique first-initial match (M. Schaffer + B. Young correctly left unlinked — not in lab_users). Grading now wired end-to-end for Fri: grader day-picker surfaces Day 2, G14's 4 groups populate, practice cases gradeable. **Teaching blocks (pmi_schedule_blocks) deliberately NOT written** — the target dates already hold G14's normal semester-2 program schedule (EMS 172/152/192, EMT Lecture, S2 Lab); overlaying 17 ACLS lesson blocks would conflict. Held for an explicit overlay/replace/skip decision. No app code changed (standalone script).
- `18473a34` | Advanced-Cert (ACLS) **content importer + megacode pool = both tiers** (Step 2; Option C). New `scripts/import-acls-seed.js` mirrors the skill-drills importer: idempotent SELECT-then-write upsert (segments by `(key, cert_course)`, scenarios by `(case_code, cert_course)`), deactivates extra criteria rather than deleting (preserves any recorded-result FKs), `--dry-run` runs in a tx and rolls back. Ran against production from `acls_scenario_seed.json`: **9 segments, 41 criteria, 20 scenarios (8 learning-station 0-4 + 8 megacode-practice + 4 megacode-test), 40 assembly rows** — all new, 0 conflicts; verified live. Grading pool widened from testing-only to BOTH megacode tiers: `listScenarios` accepts one-or-many tiers (`.in('cert_tier', …)`), `/api/adv-cert/scenarios` parses comma-separated `tier` (default `megacode_practice,megacode_testing`), grading UI requests both and labels each option `• TEST`/`• practice` + `no segments yet` when empty. Effect: the 8 practice megacodes (full 5-segment chains) are gradeable immediately; the 4 `MEGACODE_TEST_*` cards show as `0 segments` pending their live sequences (assemble-later). Day-1 0-4 cases also loaded so they can be assigned to stations (existing `scenario_assessments` flow, unchanged). `scenarios.category` set to 'Cardiac' for all ACLS rows (NOT-NULL). New `docs/ACLS_MODULE_STATUS.md` (design + decision handoff). Build green. Next: day-builder (Step 5) from `acls_schedule_seed.json`.
- `fe165b37` | Advanced-Cert (ACLS/PALS) **online megacode grading** — Step 3 of the ACLS build (deployable; Thursday target). New `lib/adv-cert.ts` (domain helpers: `listScenarios`, `getScenarioWithSegments`, `saveAttempt`, `listAttemptsForDay` — mirrors the lib/exam-scheduling split) + `types/adv-cert.ts` (incl. `ADV_CERT_SCALE` 0–4 reference constant) exported via `types/index.ts`. Four routes under `app/api/adv-cert/`: `scenarios` (pool list, derived from cert tags), `scenarios/[id]` (scenario + ordered segments + active criteria = the grading-form structure), `grading-context` (candidate testing days; per-day cohort/groups/members/stations for the selectors), `attempts` (GET by day + POST save). Grading UI at `app/labs/adv-cert/grade/page.tsx`: course → day → group → team-lead + members multi-select → drawn scenario → segment/criteria checklist + per-segment pass/fail + instructor-set overall pass/fail. `saveAttempt` persists the full graph (attempt + attempt_students + segment_results + criterion_results), is idempotent on a client-minted `client_uuid` (offline-ready from day one), and best-effort writes a `team_lead_log` row for the test team-lead (cohort_id/date derived from the lab day; skipped cleanly if no station context). Audit logged via the existing `assessment_created`/`scenario_assessment` union (module flagged in metadata). Embeds use explicit `!fk` hints for the `adv_cert_test_attempts↔students` ambiguity. Nav: Megacode Grading tile on the Labs hub (HARD requirement met). Verified: prod build green; full attempt-graph round-trip against live (2 students/2 segments/3 criteria + team_lead_log + client_uuid dedup) inside a rolled-back transaction — nothing persisted. Caught + fixed `scenarios` has no `name` column (it's `title`; aliased `name:title` in selects); noted `scenarios.category` is NOT NULL for the Step-2 importer. SITEMAP.md updated same commit. Pending: Step 2 importer (awaiting `acls_scenario_seed.json`), then offline phase.
- `421c2d34` | Advanced-Cert (ACLS/PALS) testing module — foundational schema (build-order step 1 of the ACLS build; migration `20260615_adv_cert_module.sql`, dry-run then applied). Net-new megacode-grading tables, all **uuid** PKs/FKs (the spec's BIGSERIAL was wrong for this DB): `adv_cert_segments` (reusable algorithm-segment library; importer key `(key, cert_course)`), `adv_cert_segment_criteria`, `adv_cert_scenario_segments` (ordered assembly onto existing `scenarios`), `adv_cert_test_attempts` (one group's scored run; instructor-set group `overall_result`; `client_uuid UNIQUE` for offline dedup set from day one), `adv_cert_attempt_students`, `adv_cert_segment_results`, `adv_cert_criterion_results`. Generic `adv_cert_` prefix + `cert_course` discriminator so PALS is a content load, not a second build. Tagging/grading columns added to existing tables: `scenarios.grading_model`/`case_code`/`cert_course`/`cert_tier`/`scenario_scope` (+ partial-unique `(case_code, cert_course)`), `skills.cert_course`/`cert_tier`, `skill_drills.cert_course`/`cert_tier`, `lab_days.is_adv_cert_testing` (parallel to `is_nremt_testing`, not overloading it) + `lab_days.cert_course`. CHECKs on the closed enums (`algorithm_type` left free for PALS pediatric rhythms). RLS enabled (authenticated read; writes via admin client). FK-ambiguity note: `adv_cert_test_attempts ↔ students` has both a direct FK (`team_lead_id`) and a junction (`adv_cert_attempt_students`) → grading-API embeds must use explicit `!fk` hints. Deferred to later phases: day-builder template tables, offline infra, importer/UI. DATABASE_SCHEMA.md updated same commit (7 new table sections + new columns/checks).
- `14e16978` | Seating chart front-to-back flip: also mirror within-table seat order. The default-layout print/view flip reversed row order + swapped table sides but rendered each table's 3 seats as a fixed [1,2,3] regardless of orientation, so a table viewed from the opposite side didn't mirror left↔right (e.g. "Sofia, Armand, Angelina" stayed in that order when flipped instead of becoming "Angelina, Armand, Sofia"). Flipped now renders seats [3,2,1]; non-flipped unchanged ([1,2,3]). Presentation/render-order only — seat identity, drag-drop targets, and data untouched; Suite1Layout already did this. Overflow back-wall seats left as-is (flagged). app/labs/seating/charts/[id]/page.tsx (the only live seating page; lab-management is redirect-only).
- `4f108d75` | Exam scheduler — phase-2 auto-confirm now requires a usable internship row (refinement to Piece 1). A phase-2 PM/PMD student whose internship record is missing/incomplete (so the SCHEDULED write-back can't land) is routed to PENDING director approval instead of auto-confirming — never silently unscheduled, never blocked; the director confirms and catches the data gap. `getRosterStudent` now returns `hasInternship` + `willAutoConfirm` (=isPhase2 && hasInternship); POST signups gates on `willAutoConfirm` with an informative director note ("phase 2 but internship record missing/incomplete"); student page messaging uses `willAutoConfirm`. Clean phase-2 + internship row still auto-confirms. Admin placement + approval paths unchanged. Verified: all 20 G13 students resolve to auto-confirm (phase2 + internship present); missing-row branch latent but logic-checked. No schema change.
- `c45df2f0` | Exam scheduler — SCHEDULED write-back to internship tracker (Piece 1 of completeness task; Piece 2 cohort view gated for after testing). New additive column `student_internships.written_exam_scheduled` (date, migration `20260615_written_exam_scheduled.sql`, applied). Dedicated column (NOT the legacy generic `final_exam_scheduled`). Helper `setWrittenExamScheduled()` (best-effort, most-recent-internship-row pattern matching the result/completed path) wired at the confirmed-only transitions: self-signup auto-confirm, admin approval (pending→confirmed), admin placement → set; reschedule → update; cancel → clear. Pending/phase-1 signups never set it. COMPLETED path (written_exam_passed/date) unchanged. Verified live: column writable, row-pick correct, set/update/clear behave (restored after). DATABASE_SCHEMA.md updated same commit.

## 2026-06-10

- `e1a83ada` | DATABASE_SCHEMA.md: CHECK-constraint coverage completed (doc-only). Added the 105 live-but-undocumented CHECK constraints (104 via section-aware generator across 61 tables — LVFR AEMT, OSCE scoring incl. 15 on osce_evaluator_scores, ride-along, volunteers, lab_day, PMI scheduling — + 3 exam-table entries that needed ###-header handling; 1 pre-existing multi-line entry, access_cards_single_owner, collapsed to single-line and updated to live def). Final verification: ALL 251 live CHECK constraints documented, byte-exact vs pg_get_constraintdef after whitespace-normalization, 0 mismatches, 0 missing, 0 merged lines. No database structure changed.
- `174c02de` | LVFR date-contradiction fix (A+B from the calendar-map options; C1 archive-in-place; D deliberately deferred). A: unified calendar §3 now renders ONLY published plan instances — the stale Jul 7 DRAFT plan (336 placements, abandoned 03-18) no longer shows on the master calendar (status was fetched but never checked). B: installed the correct 30-day course_calendar.json (Jul 14→Sep 17, transformed to seed-canonical fields: day_of_week, derived module_id/has_quiz/exam_module) and targeted-upserted lvfr_aemt_course_days (30), supplementary_days (10), + the 8 new modules (FK prerequisite — deviation from two-table plan, old mod1-7 left in place, flagged). Operational columns (status/completed_*) deliberately untouched. Backups: course_days+supplementary and modules JSON snapshots in gitignored data/lvfr-aemt/backups/. Verified: course_days now Jul 14→Sep 17 (module_id on 24/30), supplementary Jul 13→Sep 14, unified sim renders 0 of 336 draft placements.
- `816bd6fe` | DATABASE_SCHEMA.md: reconcile all CHECK constraints to live (doc-only, no DB change). All 146 documented CHECK definitions diffed against live pg_get_constraintdef: 4 REAL value-list drifts fixed — `google_calendar_events_source_type_check` 6→9 values (+schedule_block, +schedule_block_series, +poll_meeting — the stale doc that could re-cause the May-6 duplicate incident), `lab_users_role_check` (+agency_liaison, +agency_observer), `lab_day_roles_role_check` (+coordinator), `lab_day_templates_category_check` (+field_trip); the other 142 were cosmetic paren-wrapper diffs, normalized to exact live output so future diffs are mechanical. 0 documented constraints missing from live. Known gap noted: 105 live CHECKs (mostly on the 77 reconciliation-section tables) have no doc entries yet.
- `7cab6aef` | CalendarConnectBanner: also prompt needs_reconnect users (the calendar-map gap — the banner only fired on connected=false, so lapsed-scope users, the exact population that needed the guided reconnect during the May-June outage, never saw it). RECONNECT case shows on needs_reauth regardless of prior dismissal (broken-state condition; X still hides per-visit), with reworded copy + 'Reconnect Now'. CONNECT case unchanged (respects dismissal). Fully-connected users still see nothing. One file; wizard flow untouched.
- `4c544829` | Calendar: fix buildDateTimes malformed timestamps — the shared compose for all four assignment sync types (lab_day_role, station_assignment, shift_signup, site_visit) appended ':00' unconditionally, turning stored Postgres TIME values ('15:00:00') into invalid 'HH:MM:SS:00' RFC3339 that Google rejected with a silent 400 (createGoogleEvent returns null, sync wrappers never throw) — zero assignment-type mappings ever existed as a result. New toHms() normalizer handles both DB times and the HH:MM PROGRAM_TIME_DEFAULTS fallbacks; class-block series path (separate compose, already working) untouched. Verified on 7 input shapes.
- `db915fcc` | Calendar connection scope fix (PREREQUISITE from the calendar root-cause investigation). Connect flow now requests `calendar.events` + `calendar.freebusy` (freeBusy.query was never authorized by the old events-only grant — every connection 403'd on its first availability check and was stamped `needs_reconnect`; pushes frozen since 05-06). Callback now VALIDATES Google's actually-returned scope (granular consent can omit boxes) and refuses to overwrite stored state on a partial grant, instead of hardcoding `scope='events'`. Stale comments corrected. New `scripts/verify-calendar-scope.mjs` one-account test (refresh → freeBusy → self-deleting event push → scope-stability check). Login/sign-in flow untouched; no DB structure change; rollout = users reconnect once (one-account test first per spec).

## 2026-06-09

- `d26c7d04` | Exam scheduler doors hotfix + admin placement. Part A (nav clarity): home student card relabeled "Students sign up here"; new admin-only home card "Manage Exam Sessions" → `/admin/exam-sessions`; clinical hub gets an admin-gated "Manage Exam Sessions" entry; `/exam-scheduling` blocked-state now detects staff and routes them to the admin console instead of the "account not linked" dead-end. Part B: understated "Sign up a student on their behalf" action on `/admin/exam-sessions` + `POST /api/exam-scheduling/signups/admin` — bypasses the self-service roster/email gate (admin picks the student directly), enforces one-active-slot + capacity/Pima pool, confirms directly (admin placement = approval), fires the standard notifications, audit-logged; warns when the student has no roster email (no confirmation email possible). Poll system untouched; no schema change.
- `89912b14` | Exam Self-Scheduling (final summative WRITTEN exam) — Phases A–C of SPEC_exam_self_scheduling_BUILD.md (Phase D poll retirement deliberately NOT touched). New tables `exam_sessions` + `exam_signups` (migration `20260609_exam_self_scheduling.sql`, applied: derived seat counts, partial-unique one-active-slot-per-student, advisory-lock capacity trigger). Student page `/exam-scheduling` (roster-scoped by email↔students match, NOT role — dual-role student-instructors sign up as students; unlinked email → explicit blocked message; phase-2 `current_semester=4`+PM/PMD auto-confirms, else pending; Lockdown own-computer vs Pima-computer seat gating; free reschedule; one slot per student). Admin page `/admin/exam-sessions` (session builder, phase-1 approve/deny queue, written-exam result write-back touching ONLY `student_internships.written_exam_passed`/`written_exam_date`, audit-logged). Notifications via Resend `general` template (exempt from NREMT kill-switch): directors on signup/change/cancel, students on auto-confirm/approve/deny, non-proctor directors get proctor heads-up. Calendar: session → main shared calendar with ONLY the proctor as attendee (operator token; lapsed calendar → reconnect prompt, never silent). Nav: home "Written Exam Signup" card + Admin→Lab & Clinical card. lib/exam-scheduling.ts.

## 2026-06-08

- `ed2009ee` | Documentation foundation pass. Reconciled `docs/DATABASE_SCHEMA.md` to the live database: +77 previously-undocumented tables (full detail, generated from live introspection — LVFR AEMT, PMI scheduling, OSCE scoring, ride-along, volunteers, lab_day, etc.) in a labeled "Schema Reconciliation Additions (2026-06-08)" section, +97 missing columns inserted across 28 documented tables (incl. `cohorts.is_external_program`, `email_log` status/resend_id, `polls` scheduled-meeting cols, `student_internships` meeting/nremt cols, `students` FERPA cols), Summary updated (288→345 tables, 371→483 FKs). Added the **Schema-First Rule** (HARD REQUIREMENT, with rationale) to `CLAUDE.md` and named `DATABASE_SCHEMA.md` in the Documentation Update Rule. Refreshed SITEMAP (LVFR runsheet), DEAD_CODE_REPORT (poll-scheduler artifacts), MASTER-REFERENCE. No DB/data changes — doc-only.
- `644b8816` | LVFR AEMT runsheet rebuilt as 30-day, 3-tier model (supersedes the 27-day `5bd34391` populate). New migration `20260608_lvfr_runsheet_3tier.sql` adds `requirement` (required/optional/info), `description`, `time_label` to `lvfr_schedule_items` and `brief`/`debrief` to `lvfr_day_schedule`. Parser `scripts/lvfr-rebuild/parse-sources.mjs` reads the authoritative 30-day sources (`data/lvfr-aemt/sources/`) → reviewable artifact `data/lvfr-aemt/runsheet_days.json` (30 days, 321 items: required=121, optional=48, info=152). `apply.mjs --commit` replaced live AEMT G2 data (364→321 items, Days 28-30 added, cohort `end_date`→2026-09-17; pre-replace backup written to gitignored `data/lvfr-aemt/backups/`). Day page `/lvfr-aemt/day/[date]` now renders 3 tiers (info = no checkbox), day brief above AM + debrief below PM, fixes AM-before-PM ordering, and gates progress on required items only. AEMT G2 only (is_external_program); no other cohort touched.

## 2026-06-05

- `3b47d019` | `cohorts.is_external_program` flag added — marks one-off / partner cohorts (LVFR AEMT G2 flagged) that should be excluded from generic "all active cohorts" sweeps. Calendar/date surfaces still include external cohorts; only filter the flag in code paths that would apply EMT/PM-style behavior INTO the external cohort. Immediate retrofit: `audit-lab-day-dow-mismatch.js` filters on it (32 → 17 mismatches, AEMT G2 noise gone). Future retrofit notes in commit body.
- `5bd34391` | LVFR AEMT runsheets populated: source files moved to `data/lvfr-aemt/` (course_calendar.json updated 7/7→7/14, new schedule_build_tracker.md + instructor_assignments.xlsx); new `scripts/populate-lvfr-aemt-runsheets.js` parses both files and writes lvfr_day_schedule + lvfr_schedule_items (mapping break/lunch→'break', ch/lecture/review→'chapter', quiz/exam→'exam', lab/checkoff/station→'skills', scenario→'lab'); Track A canonical PM + Track B appended after divider. Inserted 54 day-schedule rows + 364 items into AEMT G2 cohort. Cohort `start_date` moved 2026-01-19 → 2026-07-14.
- `97be71cc` | Docs sync: CLAUDE.md adds "Development Environment (HARD REQUIREMENT)" with junction setup; CHANGELOG date headings restored per actual commit dates; PMI-MASTER-REFERENCE-2026-06-05 created with current cohort + Known Issues sections; PMI-ROADMAP-2026-03-08 gets "What Shipped May 23 → June 5" + dated Active Roadmap; DEAD_CODE_REPORT logs build-artifact + LabDayChat-stub + single-slot-station-docs cleanups; SITEMAP refreshed to 2026-06-05 with skill-drill and reauth-banner routes.
- `40004bfe` | CHANGELOG: log 5a7ddc90 (.next-old* build-junk removal + OneDrive de-sync)
- `5a7ddc90` | Remove 150,773 accidentally-committed `.next-old*` build artifacts (~99% of tracked files, ~115MB of .git bloat driving heavy OneDrive sync load); add `/.next-old*/` to .gitignore so it can't recur. Build output is regenerable and Vercel builds fresh, so no live-site impact. Also moved `node_modules`/`.next` out of the OneDrive-synced tree via directory junctions to `C:\dev-cache\pmi-scheduler` to stop sync churn (local-machine change, not committed).

## 2026-06-04

- `19661ec6` | TimerBanner: restore 30s discovery poll on null timerState (regression from f05b44eb — grading pages opened on a separate device from the lab controller never discovered the running timer). Tiers: running 5s / paused 15s / stopped null / no-timer 30s.

## 2026-06-01

- `eb86091f` | Lab day DOW mismatch safety: generation-time warning when allowed day_number maps to a weekday that doesn't carry a 'lab' block_type in the cohort's schedule (the EMT G5 Monday root cause); audit script reports active-cohort mismatches; warnings surface as alert in /academics/planner wizard

## 2026-05-28

- `150ec556` | lab_day_role calendar sync: title format reworked to "Lab — {cohort} · {title}" (role moves to description); PROGRAM_TIME_DEFAULTS for PM/EMT/AEMT replaces 08:00-17:00 fallback when lab_days.start/end is null; cohort context plumbed through admin bulk endpoint + POST insert handler. Hartnell's 27 coordinator assignments are ready but blocked at OAuth (scope='needs_reconnect') — reconnect at /settings/calendar-setup then trigger admin/calendar-sync per-user.
- `fb740ab3` | Skill drill stations get a dedicated grade view (no rubric / Platinum / scenario UI — just the drill reference + observations textarea) via new SkillDrillReference component shared with /labs/skill-drills/[id]; also fixed /api/lab-management/lab-days/[id] to select drill_ids so StationCards "Drill Reference" pill renders
- `d2caae4a` | Station docs + lab coordinator role + drill picker enrichment (3 changes in one commit due to shared touchpoints): (1) per-station file/link attachments via new station_documents table + multi-doc API; (2) 'coordinator' role on lab_day_roles + 27 Hartnell coordinator assignments through 2026-07-06; (3) skill_drill picker now shows program/duration/snippet + search + filter so imported drills with category=NULL surface properly. Import Skill Drills card moved from Content&Templates to Lab&Clinical and surfaced on /labs/skill-drills header.
- `f9cb2c54` | Timer polling: status='stopped' now stops polling entirely (LabTimer, TimerBanner, GlobalTimerBanner discovery channel still polls 60s); FreeBusy reauth banner at /settings/calendar-setup surfaces above wizard card with one-click Reconnect

## 2026-05-27

- `69aefc48` | Skill drills: structured display + JSON import (migration adds source col + program CHECK + upsert index; new /admin/skill-drills/import UI + endpoint; /labs/skill-drills/[id] reference + print view; lab day station card adds Drill Reference pill for station_type='skill_drill')
- `2e96bf3b` | LabTimer: stop polling after End Lab (clear local state on null response, auto-close modal on remote End Lab)
- `f05b44eb` | Timer polling: gate on existence (no timer → no poll; remove auto-create on mount; GlobalTimerBanner discovery 10s→60s)

## 2026-05-26

- `50c9b71b` | Evening batch: LabDayChat feature flag (default OFF) + cache header refactor + availability case-norm. LabDayChat had been adding measurable load during live labs; gated behind ENABLE_LAB_DAY_CHAT system setting so it can be turned on per-lab once the realtime stability work lands properly.
- `9c719662` | URGENT perf: cache timer endpoints (Cache-Control max-age=3) + bump poll intervals + cache FreeBusy responses. Shipped mid-lab on 2026-05-26 in response to live-lab Vercel function exhaustion.
- `fcfd4ec5` | URGENT: stub out LabDayChat to mitigate live-lab perf issue. Realtime subscriptions were churning faster than the visibility heuristic could throttle; emergency stub returned empty state while the proper backoff + feature flag was being prepared (see 50c9b71b).
- `a1f29c2d` | LabDayChat: fix attempt-counter reset + Supabase stack overflow (regression from 56a03c1d) — senderRef for stable deps, attemptRef + subscribingRef mutex, full teardown (unsubscribe + removeChannel) before each retry
- `0ecf311d` | SMC mapping review CSV: 81 smc_requirements fuzzy-matched against canonical_skills (34 exact, 25 close, 22 no_match) for operator review before any DB writes
- `b3ce7bcf` | Documentation Update Rule (CLAUDE.md) + initial CHANGELOG.md with 30-day backfill
- `9e023b52` | Template matching: day=1 fallback for single-template-per-week programs + open_shifts.title trigger

## 2026-05-23

- `49f9441c` | Project-knowledge docs: May 2026 refresh (MASTER-REFERENCE + QUICK-START + ROADMAP)
- `4b295153` | Docs: refresh SITEMAP, COMPONENTS, DEAD_CODE_REPORT for May 2026
- `808bb34d` | Cleanup: delete dead /lab-management/* duplicates (~43k lines)
- `56a03c1d` | Timer persist (correct column) + LabDayChat realtime backoff

## 2026-05-22

- `dbf6a0fc` | Costs + debrief 500 fixes; Resend Results Emails button on lab day page

## 2026-05-21

- `fa8143b2` | Grading: surface "no email on file" when Send to Student silently no-ops
- `d04f4197` | Lab day + grading bug batch (May 21 lab — criteriaRatings reset, comments-for-NS, timer, equipment, calendar/costs)
- `d51d7b33` | Feedback export/import: ISO dates, universal escaping, multi-line CSV
- `44bbd246` | Scenarios: bulk-import dedup (id → title), Update from JSON, wider station picker
- `b0a970fd` | Scenarios export: also wire into the actual /labs/scenarios pages
- `95889636` | Scenarios: Export JSON (single + bulk)
- `9e62ac30` | Lab templates: placeholder gate + audit trail (lab_day_template_audit table + trigger)

## 2026-05-20

- `2f524b08` | Scripts: one-shot direct re-import for paramedic S2 lab templates
- `e023c206` | Lab gen: Fill gaps only mode + Force Regenerate results gate
- `e0ff3455` | Lab day detail: per-day Refresh from template button
- `61f9dbba` | Lab templates: fix EMT cohort program/semester resolution (EMT G5 current_semester=1)
- `a71d8547` | Calendar sync: per-user Force Re-sync (delete + recreate)
- `841eab3a` | Lab day: cleanup button, multi-device timer overwrite, post-grade flow

## 2026-05-19

- `01dde47f` | NextAuth: drop the explicit cookies block (derive from session.maxAge)
- `407c3b53` | NextAuth: 30-day session lifetime (was 24h)

## 2026-05-15

- `212682c0` | Poll creation: fix post-create 404, harden internship API, dark mode
- `aff8a41a` | Poll creation from internship page: full builder + redirect to new poll

## 2026-05-14

- `e3c890a1` | Lab result emails: scope NREMT guard per-lab + fix email_log schema
- `0fc1d899` | Shifts calendar tile: slot-level "Need X / X available / Filled"
- `31be3673` | NREMT completion tracking — simple pass/cert-received flag
- `6450cb80` | Calendar: token-gated live ICS feed for external subscribers

## 2026-05-13

- `4e381e5c` | Master calendar: drop lab-coverage shifts + dedup lab_day tiles
- `a0910a66` | Lab groups: belt-and-suspenders no-cache on all read paths
- `85813926` | Lab groups: read paths now use lab_group_members (was reading legacy student_groups)
- `3f97a745` | Scheduling: bulk lab-shift generator for coordinators
- `711eefb5` | Lab groups: PUT /members uses UPSERT, eliminating the unique-id race
- `1016bb1d` | Lab groups: fix 500 on save (unique student_id) + sort order
- `c607490a` | Lab groups: members route now uses lab_groups + lab_group_members
- `35203dac` | Lab groups + calendar availability: error log noise fixes

## 2026-05-12

- `1e5c77fb` | Labs: 5-bug batch (protocol tracking, collapsed sections, labels, links, NREMT retake)

## 2026-05-11

- `fe3c74fb` | Clinical internships: 6-bug batch fix (notifications, nav, state)
- `0d451890` | Clinical internships: 6-bug batch fix for preceptor + date flows
- `626a1f0d` | Seating: layout-aware auto-generate + Suite 1 label fix
- `4d377ed5` | Seating chart: Suite 1 room preset (30 seats, two-section split)
- `c2d5b618` | Master calendar (/calendar): LVFR "Compact" toggle on filter chip
- `10ae96bc` | Coordinator calendar: collapse LVFR blocks to AM/PM session chips
- `f4f1d3e9` | LVFR AEMT day runsheet — flexible checklist per day
- `acbf6fd3` | Part-timer self-service shift logging — Log a Shift
- `282778d2` | Seating chart: print fits on one landscape page
- `731c57a3` | Day 1 Intake: hardcoded EMS employer <select> (no agencies fetch)
- `638eafd0` | Day 1 Intake — quick-roster auto-save table

## 2026-05-08

- `c2efb2bd` | critical_actions: stop the JSON-wrapping loop + cleanup 6 corrupted rows
- `033bcd48` | Lab-day stations: flex-wrap actions + accept content alias for versions

## 2026-05-07

- `50b93b09` | Station-edit scenario picker — bump limit so all active scenarios show
- `df9d9c37` | Bulk-import: unwrap { scenarios: [...] } + consolidate import paths
- `3caec3c0` | ScenarioGrading — use ScenarioFullDisplay; lab preview default-open
- `41840b11` | ScenarioFullDisplay — extract grading panel + reuse on lab page
- `f75e67d4` | Scenarios — pure-regex demographic extraction (no Anthropic API)

## 2026-05-06

- `64347d9d` | Extract-demographics — fix 500: timeout, top-level catch, batching
- `aa05d06d` | Reports hub — link 5 orphan reports + add 'Operational' section
- `5921497b` | Navigation entry-point rule + My Account home card
- `691f52bf` | Scenarios — cleanup UI + preferred_manikin form integration
- `cfc7c424` | Direct-DB equivalent of /api/admin/scenarios/extract-demographics
- `c38cd66b` | Scenarios — extract-demographics endpoint + preferred_manikin column
- `830ae7e3` | Direct-DB equivalent of /api/admin/seed-instructor-availability
- `9a6c8345` | Cleanup duplicates — UI button + confirmation modal
- `11fa134d` | Seed availability — exclude Kat (medical director) + LV EMS (display login)
- `dfa9f170` | Seed availability — widen role filter to include superadmin / admin
- `668cb3fe` | URGENT — duplicate-events incident root cause + cleanup endpoint
- `39eea741` | URGENT: disable calendar-sync cron via kill switch
- `ecc467ec` | Calendar filter union + station_instructors user_id backfill

## 2026-05-05

- `5842bde2` | Availability-aware lab planning — 4-group instructor dropdown + seed
- `8b85577c` | Admin sync feedback + recurring series push + UserMenu hardening
- `fe3f2bb6` | Poll → Schedule Meeting — close the missing-endpoint loop
- `a2712ada` | Calendar sync — fix instructor-block lookup + recurrence + ownership guard
- `b1058886` | Semester management UI + UserMenu visibility fix
- `fbceb022` | Lab template import — accept field_trip + surface real DB errors
- `cd099fba` | Backfill missing schedule blocks — PM G14 / PM G15 Summer 2026
- `48df3025` | Lab template import — accept both title and name fields

## 2026-05-04

- `1255937a` | Calendar onboarding — top-nav user menu + home banner + 3-step wizard

## 2026-04-30

- `b8dec3e6` | Generate wizard — prevent duplicate program_schedules + show target semester upfront
- `ee3b54f2` | Generate wizard — auto-detect semester per block + unmapped-date prompt
- `e1aaafcc` | Generate Semester Schedule wizard — direct API + URL param normalization
- `1bd89bb1` | Generate Semester Schedule wizard — fix template program key mapping

---

## Earlier history

For commits before 2026-04-30, see `git log --oneline main` directly.
The historical changelog before this file existed is reconstructable
from git history but was not preserved as a CHANGELOG entry.

This file was created 2026-05-26 with a 30-day backfill, in response
to the Documentation Update Rule added to CLAUDE.md the same day.
